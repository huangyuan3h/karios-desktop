# Karios Desktop 优化 Checklist

> 工程执行栈（OPT-xxx）：架构 / 性能 / 兼容 / 工程债怎么做。编号已扩展至 `OPT-242`（重号见文末）。
> **正文 5 条未完成 + 3 条冬眠**；已完成 117 条按天归档 [`archive/`](archive/)（见文末索引表）。

---

## 如何使用

1. 未完成队列已清空（2026-09-06 H2 收官）；新 OPT 直接在"未完成"下开节，唤醒冬眠同理。
2. 每个任务开独立 Agent 会话，把对应章节整段粘贴给 Agent 作为 scope。
3. 完成后将 `[ ]` 改为 `[x]`，填写 **完成日期**，并按天归档：新档 `archive/YYYY-MM-DD-opt-NNN-slug.md`（仿 git 历史）+ 正文该节替换为索引行。
4. 若实施过程中方案有变，在本文件更新，不要另起文档。

### Agent 任务模板（见仓库根 AGENTS.md → Scoped optimization tasks；本文件只补一条）

> 开独立会话，把对应章节整段粘贴为 scope；只改列出的文件范围；更新状态；补测试；不扩 scope。

---

## 未完成（5 条 · 2026-09-11 工程盘点立）

> 来源：2026-09-11 工程健康盘点。**一 OPT 一会话，只改列出的范围，补测试，不扩 scope。**
> 顺序建议：A → B →（按需）C/D；E 策略实现准确度建议与 B 同期。

### A 仓库卫生（高杠杆低风险）✅ 全完成（OPT-160/161/162）

### B 质量护栏（棘轮，防回退）

| OPT | 状态 | 范围 / 验收 |
|-----|------|-------------|
| OPT-163 | [ ] | **pyright 警告棘轮**：`pyproject` 现把 `report{ArgumentType,OptionalSubscript,OptionalIterable,OptionalMemberAccess,GeneralTypeIssues,AttributeAccessIssue}` 降为 warning。先统计基线数，逐模块转 error（engine/routes/paper 优先）。验收：warning 数单向下行，CI 保持绿 |

### C 性能 / 数据（按需）

| OPT | 状态 | 范围 / 验收 |
|-----|------|-------------|
| OPT-167 | [ ] | **热表索引 + 慢查询审计**：`bar_5min`(6.5GB)/`daily` EXPLAIN 关键查询、补缺失索引；前端 bundle 体积分析。验收：慢查询清单 + 索引前后耗时对比 |
| OPT-168 | [ ] | **依赖刷新**：`pnpm outdated` + `uv lock`，分批升级 + 回归。验收：锁文件更新、全量测试绿、无 breaking |

### D 产品 / 可观测（最低优先）

| OPT | 状态 | 范围 / 验收 |
|-----|------|-------------|
| OPT-169 | [ ] | **UI 极简化美化**：按 `designs/ui-minimal-redesign.md`（拍板后从 designs 转 OPT）。验收：设计稿条目落地、前端测试绿 |
| OPT-170 | [ ] | **健康聚合 / 告警巡检补强**：聚合 job 失败/数据新鲜度/熔断为单一 health 视图，巡检缺口。验收：缺口表 + 告警用例 |
| OPT-178 | [x] 2026-09-13 · **Live 前视/账本修复 + 双子星退役 + 港湾上线**：① trail/记账统一为 **T 收盘信号 → T+1 开盘成交**（`_exit_fill`；不再用 t-1 收盘记账）；② `_pnl_for` 改读 camelCase（pnl/days 不再恒 0，新增回归测试）；③ `SELL_TO_A_SHARE` 0 元平仓路径**随双子星一并删除**；④ `tests/test_sleeve_paper_auto.py` 改假 symbol + patch 候选集（不再触碰真实腿）；⑤ `paper_twin_star`/`twin_star_daily`/`twin_star_intraday`/`sat_hold_path`/`sat_push_log` **删除**，3 个调度 job、2 个 API、`source=twin_star` 停写；⑥ 卫星 replay 仅研究脚本保留，非 Live；⑦ `multi_asset_sleeve` 重写为 **Harbor P1**（去 `MIN_IDLE_PCT`/STOCK gate；无候选→REPO；idle 即停），`/timeline?strategy=harbor` 新增（数字与 B11 一致：valid +47.8/base +38.7）；⑧ Alembic `0050_remove_twin_star`（drop `sat_push_log` + behavior_audit sat 列 + `user_trades.leg` sat→parking）；⑨ 前端默认 `harbor`、双子星组件/页面/契约删除（833 前端测试绿）。**残余（另计 OPT-179）**：旧 `third_asset_sleeve`（T6 单纳指，已不再交易，仅 health/brief 展示）与 Harbor 实现收敛 | — |
| OPT-180 | [x] 2026-09-13 · **Live=回测 决策单源归零**：`harbor.pick_parking`（mom60/MA200/别名/覆盖）与 `harbor.parking_replay`（trail 优先、TS 级换仓、幻影日过滤）成为 Timeline / Live / 评估脚本唯一实现；对账 **100%（473/473，三窗；含窗口首日）**。附带修复：Live `_pick` 曾用 A 别名 mom 却返回 B 标的（永远 513110）、回测别名换仓 peak 语义。**残余（已文档化）**：回测 NAV 用 prev 收盘成交代理、Live 实际 T+1 开盘（隔夜差未建模，决策级一致；如需 NAV 级可另开 OPT）。见 [audit-live-vs-backtest-2026-09-13](backtests/audit-live-vs-backtest-2026-09-13.md) | — |
| OPT-179 | [x] 2026-09-18 · **旧 T6 `third_asset_sleeve` 退役**（随 OPT-226 一并做）：删除 `service/third_asset_sleeve.py`；`portfolio_health` 去掉 `thirdAssetSleeve`/`thirdAssetHolding` 输出（CN 排除改用 `is_multi_asset_symbol`）；`trading_brief` 去掉 execution-card 的 `thirdAssetSleeve` payload 与 paper T6 读取；`webhook_format` 去掉 sleeve 段；`allocation`/`core_holding_audit` 改指 `multi_asset_sleeve`（新增 `_etf_symbol_to_ts`）；删 `/api/backtest/sleeve-nav` + 前端 `SleeveNavCard`/`useSleeveNavQuery`；删前端 `thirdAsset*` 类型与孤儿 `ThirdAssetHealthBlock`；ai-service decision 去 `thirdAssetSleeve`。测试：删 `test_third_asset_sleeve.py`，改 `test_webhook_format`。验证：后端全量 4345 绿、前端 typecheck/ai-service typecheck/lint 0 error | — |
| OPT-181 | [x] 2026-09-14 · **港湾 Live paper 执行修正（P0 审计发现）**：① `paper_sleeve_holdings` 统一读 camelCase（`sleevePct`/`entryDate`/`tsCode`）并把分数归一为百分数（S-3 0.10→10），`_build_multi_for_paper` 与 `/commodities/sleeve/paper`、recon 共用（原读 snake_case → idle 恒 100%）；② holdings 带 `entryDate` → trail8 在 18:20 job 真正生效（原 entryDate 缺失直接 return None，recon 按同盲区复刻）；③ ROTATE 原子化：任一旧腿无 exit fill 或平仓失败则不开新腿（原 continue 后无条件 insert，可双持仓）；④ `_resolve_fill_ts_code` 让 ETF `pendingOpenFill` 占位价可回填真实 T+1 open（股票平仓路径仍不收 ETF，避免通用止损误杀）；⑤ ETF 平仓按 0.05%/边×2 记 gross/net/costs（原只记 gross）。测试 +6（sizing/trail/rotate/fill/cost）；全量 4191 passed、`db_rows_baseline.py check` OK | — |
| OPT-182 | [x] 2026-09-14 · **三策略前视审计与修正（习惯双子星/港湾/母港）**：① `eval_twin_star_parking` 卫星口径补全（`rank_key="amp_1430"`——原默认 **全天振幅旧前视键**、`exit_hhmm="1430"`、C1 3%），核心换单源 `harbor.parking_replay`（原本地旧循环，B12 core=旧数字）；② B3/母港波动率窗负索引前视（`range(i-60,i)` @i=60 → `range(max(1,i-60),i)`；`eval_harbor_riskbudget`/`eval_b3_cap`/`eval_etf_benchmark_parking` 三处，train 窗命中）；③ 卫星 R-wide 闸用当日收盘广度（14:30 前视）→ `replay_sgap_from_context`/`build_sgap_timeline` 新增 `gate_1430`（`bar_5min` ≤14:30 print vs MA20；合格域覆盖 100%）；④ Live `multi_asset_sleeve._etf_trail_exit` peak as-of 截断（原扫到 day 后 bar）+ `_signal_series/_signal_closes/_pick(as_of=day)`（历史 recon/展示不再取墙钟最新）；⑤ ETF 平仓补完 OPT-178①：`_exit_fill` 标 `exitPendingOpenFill`，新增 `list_pending_exit_fills`/`patch_paper_exit_fill`，`run_update` 次日回填真实 open 并重算 gross/net（原占位价即最终价、永不回填）；⑥ ROTATE 新增 `parkPct`（新腿不再 0%）+ recon `_opened_by_sleeve_job_today` 日期门槛（历史 BUY 腿不再被剔除、当日 open 平仓腿不再算持仓）。⑦ **卫星价格基期混用（qfq×raw）**：`daily` 于 2026-09-11 被 `cn_reseed_qfq_tx.py` 重灌为前复权，`bar_5min` 仍为原始价——`same_1430` 老口径「收盘卖」= qfq_close/raw_entry（系统性亏损 + −80% 假回撤），`gate_1430`/C1/涨停保护跨基期比较失准。修复：`state_bucket_track` 新增 `_mark`/`_raw_ratio`（raw 15:00 记账、qfq open/pre_close 按当日因子缩放、广度 MA 用 raw），`next_open`/`same_close` 老口径不动。校验：1041/1041 入场、993/993 出场经因子换算后落在日线 [low,high] 内。**结论**：双子星 B12 叙事作废、基期统一后卫星 long **+439.0%/MDD −9.6%**；双子星 **+145.2/+50.5/+25.5/+329.1**（Δcore +83.2/+5.2/**−24.8**/+109.2）→ 仍 REJECT（只挂 valid）；港湾不变、母港 PASS；Live 仍=港湾。测试 +19（新 `test_eval_riskbudget_causal` + gate/raw-basis/C1 缩放/trail/parkPct/exit/recon）；全量 pytest + `db_rows_baseline.py check` OK。**注：本条数字经 OPT-183 日历修正后更新**（clean：卫星 long +463.6%、双子星 +149.7/+52.2/+29.1/+291.9），结论不变。见 [audit-three-strategy-lookahead-2026-09-14](backtests/audit-three-strategy-lookahead-2026-09-14.md) | — |
| OPT-183 | [x] 2026-09-14 · **双市场日历污染修复（HK-only 日期）**：`daily` 表 CN/HK 同表且无市场过滤 → `backtest_engine._load_calendar` 返回 CN∪HK 并集（long 窗多 57 个 HK-only 日 = CN 假期，`bar_5min` 0 行、`index_daily` 0 命中）→ S-3 持仓天数/换手门槛/结算/cooldown 被加速；卫星在假期强平无 mark 持仓（PnL 归零，48 笔）。修复：`backtest_engine._load_calendar(start, end, market)` 按 `config.market` 过滤（CN `NOT LIKE '%.HK'` / HK `LIKE '%.HK'`，BacktestData 传 market）；`state_bucket_track._load_calendar` 固定 CN-only。测试 +2（`test_backtest_engine_loaders::test_loader_calendar_filters_market`、`test_state_bucket_track::TestLoadCalendar`）。**重固化**：CN 官方基线 +38.0/+39.2/+38.7（long V0 +82.7）、HK 线 +21.8/−0.2/+65.7；港湾 P1 +55.2/+52.2/+50.3/+201.5（Δ +17.2/+13.0/+11.6/+118.8，K1–K3 仍 PASS）、双子星 +149.7/+52.2/+29.1/+291.9（仍 REJECT）、母港 M50 +36.5/+35.0/+25.6/+116.3（仍 PASS）；report JSON 全部重生成。见 [audit §F](backtests/audit-three-strategy-lookahead-2026-09-14.md) | — |
| OPT-203 | [x] 2026-09-15 · **对账/卫星显示修复 + 实时「账户 vs 回测」跟进面（用户反馈）**：① **研究面板 CSV 月更造成假滞后**：`harbor.load_etf_closes`（停车）/`homeport.load_risk_closes`（B3）优先读 `data/etf/etf_daily.csv`（每月 1 日全量刷新，而每日 17:25 `sleeve_etf_daily_sync` 只写 `daily`）→ 港湾家族 Timeline 停在 09-11、B3 卡「截至 09-11」；新增 `harbor.merge_recent_db_closes(out, wanted)`（`close × COALESCE(adj_factor,1)`，只追加各序列 CSV 末日之后的 `daily` 行，冻结历史不动、失败仅告警）→ 实测 harbor 行到 **09-14**（pick OIL）、B3 asOf **09-14**。② **卫星槽位标签越界**：`satSlots` = 持仓 + 当日平仓（换手日 4+4=8 > 容量），`SatelliteLegBlock` 改 `有仓 n/{satCapacity}` + `今日换 k`；行加 `satCapacity=MAX_POS`（`state_bucket_track` 行 + `sgap_to_timeline_rows` + `starport.blend_overlay_timeline` 拷贝 + TS 类型），`gateOpen` 缺失显示「—」（原默认「开」）。③ **卖出日对账假红牌**：`_user_alignment` 原来只给买入 T+1 容忍（卖出永远 `missing`，用户次日开盘平仓后仍红）→ 卖出同样走 `exitExecDate`（未到执行日 `pending`、执行日已记账 `aligned`），`userSells` 同步纳入执行日。④ **实时跟进面**：`SleeveReconBlock` 重写为三方对照——`策略（label/目标/实持）· 镜像盘（✓ 或 缺买/缺卖/多开）· 你的账户（无操作/✓已跟上/⏳待执行 {执行日} 开盘买 X/🔴未执行 应买X 应卖Y）` + 明细三行（策略/镜像/你 的 flows + 执行日）+ `refetchInterval 60s` + 「实时 HH:MM」；股票篮对账补「对齐票中位差 pt（我 − 回测）」（字段早已返回、此前未渲染）。测试：后端 `test_etf_closes_freshness`(3) + 卖出容忍(2) + satCapacity 断言；前端 `SatelliteLegBlock.test`(4) + 对账卡用例改写；前端 **850 passed** / typecheck / lint 0 error、后端子集 55 passed。**需重启后端生效** | — |
| OPT-204 | [x] 2026-09-15 · **整数手换算 + 排名微调（用户下单反馈）**：买入弹窗此前只给「仓位 %」，实际只能按整手下单。新增 `apps/desktop-ui/src/lib/lot-sizing.ts`（纯函数单一真值）：`lotRuleFor` 编码交易所规则 **沪深 100 股整数倍 / 科创板(688) ≥200 股+1 股递增 / 北交所(43|83|87|88|92) ≥100 股+1 股递增 / ETF 100 份 / 港股每手（常用 17 只静态表 + 默认 100，标注以券商为准）**；`roundToLot` 取最接近的合法手数；`suggestLotSizing` 按 `总资金 × 目标% × 排名加成 ÷ 价格`（港股先按 `HKD→CNY` 汇率换汇）给出股数/实际%/1 手金额，**股票 ≤15% 集中度上限、ETF 停车不受限**；`rankSizeBoost` = 排名 #1 ×1.2 / #2 ×1.1 / #3 ×1.05（用户「排名靠前容易赚钱的略微多买」——**仅下单换算建议，不改变策略仓位规则**，要变成 Live 规则需另起预注册 + 三窗）。账户输入：`lib/account-settings.ts`（localStorage `karios.accountCapital.v1` / `karios.hkdCnyRate.v1`，默认汇率 0.92，事件同步）+ Settings 新「账户」tab（`AccountSettingsPanel`）+ 弹窗内联「总资金」快捷输入（`LotSizeHint`，未设置时出现）。接线：`QuickBuyDialog`（带 `rank`，BUY 侧）、`TradeActionDialog`（buy/add）、`PortfolioHealthCard` BuyList 传 `i+1` 排名。测试：`lot-sizing.test`(13) + `LotSizeHint.test`(3) + QuickBuyDialog 集成(1)；前端 **869 passed** / typecheck / lint 0 error。**纯前端（无需重启后端）** | — |
| OPT-205 | [x] 2026-09-15 · **星舰 v2 停车套筒成交审计（用户反馈）**：Timeline「星舰 · v2」此前只有卫星腿明细（持仓 + 成交流水），闲钱停 ETF 的买卖（如买黄金）只体现在 NAV 曲线里、没有成交记录。新增 `state_bucket_track.parked_blotter(recs, closes, names)`（纯函数）：从 canonical `harbor.parking_replay` 记录还原买卖事件——**成交日 = 记录的 `prev`（引擎按 14:30 收盘代理成交）**、换仓 = 卖旧 + 买新两条（`rotate`）、别名切换（NASDAQ 513100↔513110）同为换仓、trail8 出场只卖不当天回补（`trail`）、无候选回现金（`cash`）；`apply_parked_display` 输出 `parkedBlotter` + `parkedHeld`（含 `weight` = 期末停车权重）+ `summary.parkedTrades/parkedTrailExits`。前端 `TimelineParkedBlotterRow/TimelineParkedHeld` 类型 + 星舰页新增「停车套筒明细（闲钱买 ETF）」卡：当前持有（名称 + 代码 + 自/现价/停车权重）+ 近 30 日/全部成交流水（成交日/动作/标的/价格/原因）+ 买 N / 卖 N / 回撤出场 N。测试：后端 +3（事件顺序与价格、持有腿与回现金、apply 接线）、前端 BacktestPage 用例 +1；前端 **870 passed / 1 skipped**、typecheck / lint 0 error、ruff clean。**需重启后端生效（API 字段）** | — |
| OPT-206 | [x] 2026-09-15 · **Watchlist 停车逻辑与引擎单一来源（用户要求「保证 watchlist 逻辑一致」）**：① **后端**：`multi_asset_sleeve.CANDIDATES` 原为本地 ETF 宇宙（与 `harbor.MULTI_TS`/`NASDAQ_ALIASES`/`NAMES` 各自维护）→ 改为派生；`TRAILING_PCT = harbor.TRAIL_PCT`（原第二个本地 `8.0`）；新增 `multi_key_for_symbol()`（`ETF:513100`/`513100.SH`/`513100` 三形态 → 引擎 key），`portfolio_health.multiAssetHoldings[]` 带 `key` 字段（前端不再自建映射）。② **前端关键逻辑修复**：`PortfolioHealthCard` 原 `allowStockBuys = pickKey === 'STOCK'`、`rotateOutCn/Hk = pickKey !== 'STOCK'` —— 港湾口径下 pick 恒为停车 ETF/REPO，会**静默禁用股票篮买入**并把核心持仓标成「应轮出」；改为 `allowStockBuys = sleeve.s3BuySetup ?? (候选>0 && 闸开)`、`rotateOut* = false`（核心出场只由后端逐仓 S-3 动作决定），停车只停闲钱。③ **文案/映射统一**：新增 `lib/parking-universe.ts`（`PARKING_META`/别名/`parkingKeyForSymbol`）为前端单一来源，`MultiAssetHealthBlock`/`PortfolioHealthCard`/`replica-gap`/`RecentDailyCompareCard` 全改用它；清除「核心 ETF」「100% 硬切」「mom_compare 定案」「目标 100%」等旧口径（操作引导/停车体检/replica-gap/dashboard+watchlist 导出）。实测（真数据 09-14）：`pick=OIL 513350 mom60 32.18` 与 Watchlist/停车审计一致、5 个 ts 全部 round-trip。测试：后端 `test_parking_universe.py`(4) + 相关 90 passed；前端 `PortfolioHealthCard.test` 语义改写（ETF pick 不再禁用核心买入）+ 全量 870 passed / typecheck / lint 0 error、ruff clean。**需重启后端生效（`holdings.key`/`parkPct`）** | — |
| OPT-207 | [x] 2026-09-15 · **星港/星舰/双子星「怎么调」操作提示 + 卫星书透传（用户：看不到切换路径）**：三档研究视图此前只显示卫星腿状态（槽位/闸/最近交易日），没有可执行提示。① 后端透传：`build_sgap_timeline` 顶层加 `satCapacity`；`starport.blend_overlay_timeline` 透传 `satWeight`/`baseKey`/`satCapacity`/`openPositions`/`blotter`（原只拷贝 rows/summary → 星港/双子星前端根本没有腿数据）；starship 路由补 `satWeight=1.0`/`baseKey=satellite`。② **修复 `exitDue` 窗口截断**：`open_positions` 在 due 超出窗口时原回退 `last_day`（09-11 入场的 4 只显示 09-14），改为优先用建仓时存好的 `p["exit_due"]`（= 请求 `end`）→ 实测显示 **2026-09-15（今天）**。③ 前端 `SatelliteLegBlock` 升级为「卫星腿 + 操作提示（研究档 · 不进 Live）」：结构/卫星权重（星港 1/3 · 双子星 1/2 · 星舰 100%）· 每槽 % · **14:30 到期卖出清单**（余 1 日）· 继续持有清单 · 补仓条件（闸现场判定 + 跳空/振幅/T+1 过滤 + 空槽）· 星舰闲钱停车行（`parkedHeld` + 停车权重）· 跟法（切换路径）；`PortfolioHealthCard` 传 `strategy/satWeight/openPositions/parkedHeld`。④ 文案清理：操作引导删「100% 硬切 · 定案 mom_compare」；闲置/停车目标按动作区分（HOLD 不再显示「闲置 100% → 买入」）。测试：后端 starport +2；前端 SatelliteLegBlock 6(+2)、PortfolioHealthCard 7(+1)、全量 **873 passed / 1 skipped**、typecheck / lint 0 error、ruff clean。**需重启后端生效** | — |
| OPT-208 | [x] 2026-09-15 · **星舰 Timeline 日表记录停车 ETF（用户：只有「停车 30%」看不到黄金）**：逐日明细只有停车权重、没有停的哪只 ETF。① 后端 `apply_parked_display` 从同一份 `parking_replay` 记录给每行补 `parkedPick`/`parkedTs`/`parkedSides`/`parkedRetPct`/`parkedTrail`（`REPO` = 现金）；② 前端日表「持仓」列改 `停车 {黄金/原油/纳指/国债/现金} {w}%` + 动作标记（建仓/换仓/回撤8%→现金/清仓转现金），单元格 `title` 汇总标的代码、权重、当日收益 + 当日申赎（读 `parkedBlotter`，如「买黄金 518880.SH @5.12」）。实测（1 年窗）：末行 `停车 原油 513350.SH 36%`；测试：后端 parked 6 passed（+逐行字段断言）、前端 BacktestPage 16 passed（新增日表记录用例）+ 全量 **874 passed / 1 skipped**、typecheck / lint 0 error。**需重启后端生效** | — |
| OPT-209 | [x] 2026-09-15 · **automation = 策略池（S-3 + 星舰）· 失效自动移除（用户拍板）**：原 automation 加的是 Alpha 催化/研报（与仓位无关），TV 漏斗表已停更一个月。改为维护**两条回测腿的池**：① `paper_s3.build_s3_candidates(gate_mode="pool")` 新口径 = **score≥65 & RS 过线**（CN 0.5 / HK 0.6、剔除 300 板），**不含 regime 闸**、不排除持仓名，按 (score, RS) 排名取引擎建仓上限 10/市场（实测 09-14 全过线 541 名 → 必须排名收窄）；② 星舰池 = `state_bucket_track` 习惯 replay 当前持有的 14:30 卫星腿（带 `entryDate/exitDue/daysLeft`）；③ `compute_pool_removals`：S-3 连续 2 日不在池（`S3_POOL_FAIL_DAYS`，持仓豁免）→ 删、星舰 replay 出场 → 删；manual/legacy 行永不被 GC；④ `apply_pool_run` **自动应用**（merge → `upsert_registry` → `ack_run`），前端去掉 pending/ack（`runManualAutomation` = 触发 + hydrate；`useWatchlistAutomation` = 看最新已应用 run → 刷新 registry）；⑤ **Alpha/研报渠道停用**（`ALPHA_CHANNEL_ENABLED=False`，历史行保留）；⑥ 前端：`WatchlistSourceSchema` += `s3`/`satellite`、`FunnelHistoryTable` → `PoolHistoryTable`（S-3 池/星舰/加入/移除，读 `meta.poolAdded/poolRemoved`）、`db.list_recent_runs` 过滤 `meta ? 'poolAdded'`、诊断面板文案。文档 `docs/modules/watchlist.md` §盘后自动化按现状重写。测试：后端 `test_pool_automation.py`(10) + `test_paper_s3` 池口径(3) + automation 4 用例改写；前端 869 passed / typecheck / lint 0 error、shared 83 passed、ruff clean。**需重启后端生效** | — |
| OPT-210 | [x] 2026-09-15 · **Timeline 三档（星港/星舰/双子星）流水"近 30"显示最旧 30 条 + 陈旧缓存致星港卫星书不一致（用户反馈"缺失不同的记录"）**：① 前端两处明细卡 `[...].sort(desc).slice(-30)` 取到 **最旧** 30 条 → `slice(0, 30)`（卫星成交流水 `SatelliteLegDetail` + 停车套筒流水 `ParkedLegDetail`）——用户看到的"缺失记录"= 最近的成交/申赎从未出现在"近 30"里；② 卫星腿明细副标题硬编码「v2：闲置现金停 ETF 套筒」→ 按策略渲染（星舰 v2 / 星港·双子星「组合权重 1/3|1/2（卫星 v1 · 冻结口径）」），贡献% 列在叠加档标「腿内」；③ 日表脚注与表头「基线」：叠加档（星港/双子星/星舰）不再显示与自身无关的 S-3 基线，脚注按策略给说明；④ **后端内存缓存陈旧**：重启前同窗同口径 `星港` 卫星书 2039 条 vs `星舰/双子星` 1572 条（rank/skip 全不同）；重启 4330 后端清缓存后三者统一 **1125 条（fill 204 / skip_c1 178 / skip_t1 739）**。测试：前端 BacktestPage 18 passed（+3：两卡最新 30 排序回归、权重/口径副标题 + 腿内贡献）+ 全量 **871 passed / 1 skipped**、typecheck / lint 0 error。**改动纯前端 + 一次后端重启（无后端代码改动）** | — |
| OPT-211 | [x] 2026-09-15 · **Timeline 五档全腿加载（用户：每档都只看到部分）**：① **后端**：`harbor.build_harbor_timeline` 输出 `parkedBlotter`/`parkedHeld`（复用 `parked_blotter`，held 权重=期末闲置比）+ 逐行 `parkedSides/parkedRetPct/parkedTrail` + `stockSymbols` 去 3 截断；`homeport` 抽 `risk_budget_run`（NAV 与旧函数逐值一致）+ `blend_homeport_timeline` 输出 `riskUniverse/riskBlotter/riskHeld`/逐行 `riskTop/riskTopW`/`summary.riskPct/riskMaxDdPct`（名称单源 `strategy_today.B3_LABELS`，lazy import 避循环）；`starport.blend_overlay_timeline` 透传 parked/risk 五键（summary 经 spread 自动继承）。② **前端**：`harbor-segments` 新增 `hasBaseLeg/hasRiskLeg/hasParkedLeg` + `buildHarborSegments{ignoreSat,meta}` + `buildParkingStrip/buildRiskStrip` + `harborHoldLine` 双腿/B3/停车名；Timeline 主 bar（叠加档=底座腿）+ 卫星/停车/B3 细条 + 图例按腿分开 + 日表底座·卫星堆叠 + 停车/B3 列 + 叠加档超额恢复真实值 + `ParkedLegDetail` 去星舰门控 + 新 `RiskBudgetLegDetail`（当前权重 + 月度调仓表）+ 标题 B3 累计。测试：后端 +11（harbor 腿 2/homeport B3 腿 5/overlay 透传 3/`test_api` 五策略透传 1）+ 相关子集 106 passed；前端 BacktestPage +4（四档腿断言）/segments +5 + 全量 **880 passed / 1 skipped**、typecheck / lint 0 error、ruff clean。**需重启后端生效（API 字段）** | — |
| OPT-212 | [x] 2026-09-15 · **回测算法审计修复（用户：查 bug/前视/结论坚实度）**：① **BUG-1（实锤，悲观方向）**：S-3 引擎同日进出时序倒置——`next_open` 建仓（次日 open）当天就被出场评估（当日 close），合成探针复现幽灵交易（信号日买次日价、当日价卖出 −5.96%）。修：holding==0 跳过出场/peak/金字塔 + MTM 按成本标记（初版"剔出 MTM"曾造假 100% 回撤曲线，已纠正为 at-cost）。回归 `test_engine_entry_timing.py`(4)。**影响≈0**：三窗交易集合与基线完全一致（total 小数点后三位全同），幽灵条件（隔夜 ≥5%）窗内零触发。基线已重固化（CN+HK，`walk_forward_baseline_20260915`）。② **BUG-2（口径钉死，数字不动）**：母港月度再平衡按单边收费，与字面双边差 ~0.1%/年——冻结数 churn 代价远大于修正价值，docstring 双边钉死（service+eval 各一份）；星舰 `\|Δw\|` 单账户单笔转移，核实单边正确，加注。③ **卫星幸存者偏差（量化）**：`_load_rows` 加 `include_listed_history`（退市票上市段，时点正确）/`include_st` 开关（默认全关=冻结口径）。实测 habit：valid +0.9 / OOS2 +4.5 / train +8.3 / long +24.6pt（+5%相对，不翻转结论；方向反直觉是组合效应）。ST 纳入跑出 +30pt **幻觉**（5% 板在 10% 模型下零振幅霸榜、不可成交）——反证 ST 必须继续排除，真修正需时点 ST+5% 板建模（另起 OPT）。④ **真雷（数据漂移）**：Timeline 星港 valid 13.23 vs 冻结 20.7——先修 `blend_overlay_timeline` **吞首日 bug**（循环从 i=1 起，首日 +4.2% M50/原油日被吞；回归 2），修后 Timeline==eval（17.99）；剩余 −2.7pt 系 09-15 晚 baostock 5min 回填覆盖 live 14:30 prints（旧引擎 stash 对照同为 18.01，S-3/B3/港湾基线纹丝不动，排除代码）。后果：**同一 K1 门下 chosen=1/3（−7.6）已不清，0.2（−4.6）才过**——冻结 PASS 是 09-14 数据版本的诚实记录（不改），加 §8 addendum；展示/落地改用 0.15–0.25。防再犯：`upsert_5min_payload(on_conflict="nothing")` + 两回填脚本已切（测试 3）。⑤ HK 回填宇宙改窗口内成交量（历史残余留档，重跑另计）。测试：后端新增 entry_timing 4 + universe 6 + bar_5min 3 + starport day-1 2；ruff clean。**需重启后端生效** | — |
| OPT-213 | [ ] **门控 v2 工具缺口（`validation-gates-v2-2026-09-16.md` §10；v2 先走人工+实验文档贴计算行）**：① `run_walk_forward.py --verdict v2`——自动算 G1a 三窗等权合计/各窗余量/G3a 符号一致/G3b 铰链/G3d 2倍成本并输出 PASS/条件PASS/REJECT（含条件原因行）；旧 `>5pt` 票决保留为 `--verdict legacy`（默认 v2）。② report 数据版本记录器（L5）：关键表行数 / vendor 分布 / bar_5min 覆盖 / git tag + baseline sha 自动进 report JSON。③ L-pattern grep 清单进审计：禁 `trade_date >= end` + vol 排序选宇宙、回填 `DO UPDATE` 默认、`_load_rows` 复制缺 market 过滤。验收：`--verdict v2` 在星港 1/3 当前数据上输出 REJECT（valid 破地板）、在 H-SAT-W w=0.10 冻结数上输出 条件PASS（余量）；单测覆盖 G1a/G1d/G3b 计算。 | — |
| OPT-214 | [x] 2026-09-16 · **三档 lineup 落系统（角色标记 + 回测/watchlist 排序）**：① **后端**：`strategy_catalog` 重排 [星舰/星港/母港/港湾] + `role/roleLabel`（进攻/均衡/防守/Live底座）+ 母港行换 M30 数（70/30，doc→tune 档，timelineStrategy→homeport_m30）+ 港湾 statusLabel「Live · 日落」+ 删双子星行；`homeport.blend_homeport_timeline` 加 `w_harbor` 参数（默认 0.5 冻结）+ summary `harborWeight`；路由新增 `homeport_m30`（70/30，M50 线冻结不动=星港底座；twin_star 后端保留冻结）。② **shared**：`strategyCatalog` 加 role/roleLabel（必填）+ timelineStrategy 枚举加 homeport_m30；`StrategyCatalogKeySchema` 去 twin_star。③ **前端**：`TimelineStrategy` 加 homeport_m30 去 twin_star（编译器引导全触点）；回测 tab 排序 [星舰/星港/母港M30/港湾]+字幕/注记/表注；目录面板角色徽章 + catalogKey↔timeline 映射（母港slot→M30线）；watchlist ModeBar 跟 MODES 排序 + tag 即角色；SatelliteLegBlock/PortfolioHealthCard/PickStrongAlignBanner 去 twin 分支；strategy-settings 去 twin（4 档重排 + M30/日落文案 + twin 存储迁移到星港）。星港权重保持冻结 1/3（0.2 未拍板，不动）。测试：后端 catalog 重写 + homeport M30×3（含路由直测）+ api 回归；shared 8；前端全量 **881 passed / 1 skipped**、typecheck/lint 0 error、ruff clean。**需重启后端生效（catalog/M30线/roles）**。同日追加（用户拍板）：星港默认 dial 1/3→**0.2**（当前数据验收最大权重；`STARPORT_DEFAULT_WEIGHT`，SAT_WEIGHT=1/3 冻结保留可调；目录行换 0.2 数+六年全正证据；前端 1/3 文案/权重显示同步；eval 加逐年绝对+逐月 dump 纯上报，重跑 chosen=0.2 不变；全端测试绿） | — |
| OPT-215 | [x] 2026-09-16 · **港湾H2验证线进回测系统（H-HARBOR-H2 落地，不含 watchlist 决策链）**：① **后端**：`parking_sleeve.blend_harbor_h2_timeline`（港湾 Timeline 行 + H2 停车重算，引擎/idle 原样复用；mode harbor_h2/strategy 港湾H2/验证中注记）；路由 `harbor_h2`（派生自缓存港湾线，无二次引擎；M50/星港底座与 Live 路径不动）；shared `TimelineStrategy` + harbor_h2。② **前端**：对比页 TimelineCard 经 `extraStrategies` 追加「港湾H2」tab（产品 tab 保持三档+港湾日落不变；目录 mapping harbor_h2→harbor slot）；字幕/注记/表注。③ **验证**：OOS2 线 +58.71 vs eval +58.7（逐数一致，Live 港湾 +55.21 不动）；后端 parking_sleeve×6 + api 回归；前端全量 **882 passed / 1 skipped**、typecheck/lint 0 error、ruff clean。**watchlist 决策/下单/对账链一字未动**（直连实盘指令，H2 进 Live-track 需 paper+授权另行拍板）。**需重启后端生效** | — |
| OPT-216 | [x] 2026-09-16 · **H2 Live-track：港湾H2影子账本日更 + MDD 监控 + 回滚线（只看不动手）**：① **后端**：`service/harbor_h2_shadow.py`（paper NAV 账本：S-3引擎/idle复用港湾线、停车腿H2重算；paper窗口自inception按1.0计，不掺回测史；回滚线=累计落后>2pt或回撤深过Live>1pt，观察线=落后>1pt/深过>0.5pt；原子落盘 `data/backtest_reports/harbor_h2_shadow_latest.json`，保留120日）；调度 `harbor_h2_shadow_job`（工作日18:35，重算 trailing-year 港湾线+H2混合后按日追加，幂等；失败只进收件箱低 severity、不打电话）；注册+18:40 catchup；`SYNC_JOB_TYPES` + shared catalog（indexMacro）。只读路由 `GET /api/backtest/harbor-h2-shadow/latest`（首跑前404）。② **shared**：`harborH2Shadow.ts` Zod + export + 单测。③ **前端**：对比页 `H2ShadowFollowCard`（状态徽章跟踪中/观察/已回滚/数据过期 + H2-vs-Live NAV/领先pt/当日动作/回撤对照/回滚线 + 每日三步；不读portfolio-health、不调任何写接口）+ `useHarborH2ShadowQuery`。④ **边界**：`paper_trades/user_trades/watchlist` 决策/下单/对账链一字未动；H2永不下单。测试：后端影子账本×10（含job无DB/无网+路由404/ok）+ 调度注册集 + api 回归；shared 86；前端全量 **884 passed / 1 skipped**、typecheck/eslint 0 error、ruff clean。**需重启后端生效（18:35首跑才有账本）** | — |
| OPT-217 | [x] 2026-09-16 · **watchlist 每日路径 UI（今日待办 A + 状态可操作化 B）**：① **今日待办卡** `TodayTodoCard`（页顶：闸门→缺买缺卖→待执行→持仓EXIT→对账缺口→候选→提醒有序清单；上海时钟时间感知 copy；每项"去处理→"深链到 sleeve-recon/holdings/recon/buy-list/buy-reminders；全清显示✓；只读，不新增请求，失败可重试）；BuyList 标题时间感知（14:30前/尾盘/周末）；`buy-list`/`buy-reminders` 锚点。② **可操作化**：健康卡无数据态加重试；toolbar 恢复趋势更新时间+自动化状态行、跳过中文+强制跑一次说明；对齐横幅已对齐显✓（加载中可区分）；空表中文；H2 卡过期改上海时区。测试：新增待办卡×4 + 对账弱市可见×1，更新对齐/toolbar/买入标题文案断言；前端全量 **889 passed / 1 skipped**、tsc/eslint 0 error（经手文件 0 warning）。 | — |
| OPT-218 | [x] 2026-09-17 · **五策略审计修复（用户：查 bug/前视/执行性/逻辑）**：① **H2 语义 bug（critical）**：H2 机 band 阻断时清仓去 REPO（多一次卖出+空仓一天），预注册写的是"继续持有旧腿"；eval 副本与服务同 bug（parity 互证≠正确）。修：`hysteresis_parking_replay` 阻断保持旧腿 + `hyst_blocked` 标记；`eval_sleeve_tune` H 臂单源到服务（V/B 同步改为持有）。重跑：星舰 A2_H2 long **+50.1**/valid **−0.3**/MDD −30.5（原 +137.7/+7.9/−26.1 全部作废，仍过混合门）；港湾 P1_H2 **HK3 −1.1（B 门 REJECT，差 0.1pt**；v2 待 OPT-213）；目录/两份 H2 文档/SUMMARY/前端文案按修正数重写。② **ETF 尾部拼接基准断裂（critical）**：`merge_recent_db_closes` 把 raw close 接在复权 CSV 后（ETF `adj_factor` 全 NULL）→ 实测 513100 −80%、B3 假单日 +17.62%。修：按锚点（adj_factor 或同日重叠比例）缩放尾部；`eval_sleeve_tune._load_px` 同源；测试 3。③ **引擎前视/执行 3 处**：next_open 缺分时回退查**实时 DB**（可读到模拟日之后的分数）→ `_pick_close_reason(skip_live_score_lookup=True)`（HEAD 起就红的 `test_simulate_score_floor_uses_as_of_score` 转绿）；A股 **T+1**（成交日按当日 close 出场）→ 丢弃非 force 出场理由；**next_open 涨停闸基准日**改信号日收盘（`base_day`，仅 next_open）。三窗 walk-forward 全"持平"（三处窗内零触发）→ 基线无需重冻。④ **时间线缓存**：内存缓存加 15min TTL（`_timeline_mem_get/put/drop`）；H2 影子 job `force=True` 防盘前 warmup 旧数挡住补跑。⑤ **双子星恢复**：shared key enum + `TimelineStrategy` + strategy-settings（5 档）+ catalog 行（并行对照/对照徽章，B12 冻结数）+ 回测 tab + watchlist 三组件分支 + 测试还原（用户：不退役）。⑥ 附带：contract 测试补 harbor_h2；H2 影子 inception rebase（首行=1.0）+ 回滚线 2pt 对齐文档；`test_pool_automation` 时钟 mock；`test_etf_closes_freshness` 适配锚点查询；删旧 H2 影子报告（重 seed）。测试：后端全量 **4330 passed / 3 skipped** + DB 基线 27 表零变化、`ruff check src tests` clean；前端 **889 passed / 1 skipped** + tsc 0 error；shared 87。**需重启后端生效** | — |
| OPT-219 | [x] 2026-09-17 · **审计 P2-2 收尾（三处 Live/口径缺陷）**：① **星舰池时序**：17:30 `watchlist_automation` 建池时当日 14:30 分时尚未落库（`bar_5min_close` 18:40）→ 当日新进的卫星腿迟到一天才进 registry。修：`bar_5min_close` 存完当日 bars 后调用新 `watchlist_automation.refresh_satellite_pool`（`trigger=post_5min`，幂等，只动 `source=satellite` 行，累加当日 17:30 run 的 S-3 计数进 meta）；`bar_5min_close` 进启动 EOD catch-up（guard ≥18:45）。顺带修 fail-closed 隐患：replay 失败原返回 `[]` → 移除层会把全部星舰行当"已出场"删除；现返回 `None`（区别于"当日无腿"）→ `compute_pool_removals` 整段跳过（fail-open）。② **对账把停车 ETF 当多**：paper 书里停车腿是 `market='CN'` 的 `ETF:xxx` 行，每周 `backtest_paper_recon` 的 CN 桶每个停放日都报 extra=1。修：`reconciliation` 用 `multi_key_for_symbol` 识别停车家族（`ETF:`/`.SH`/裸码三形态），`_paper_holdings_on` 与 `reconcile_registry` 均排除（停车腿由 `sleeve_paper_recon` 单独对账）。③ **Live 停车基期统一**：`multi_asset_sleeve` 原用 raw `daily`（ETF adj_factor NULL）喂 `pick_parking`/trail，与冻结引擎的 `harbor.load_etf_closes` 复权口径不一致 → 改 `_adjusted_series`（含 60s 缓存，raw 仅 fallback），并把 trail 峰值参考对齐引擎口径（含 fill 日前一交易日收盘，即换仓信号日收盘）；`scripts/verify_harbor_live_vs_backtest.py` 改用引擎基期喂两侧 → **473/473（100%，三窗）**。测试：单测新增/更新 12（pool refresh 成功/失败、fail-open 移除、bar job refresh 隔离/非交易日跳过、recon ETF 排除、trail 信号日峰值、基期 fallback 等）；后端全量 **4346 passed / 3 skipped**、DB 基线零变化、ruff clean。**需重启后端生效**；其他机器拉代码后跑 `PYTHONPATH=src alembic upgrade head` | — |
| OPT-220 | [x] 2026-09-17 · **「跳过的 run ≠ 已跑」（用户：watchlist 池子空了两天）**：09-15/16 的 17:30 `watchlist_automation` 因 `close_sync_not_ready` 跳过（那两天 close_sync 19:40/22:01 才落地），跳过的 run 在 `sync_job_record` 记为 `success=True` → 启动 EOD catch-up 与 18:05 `paper_chain_watchdog` 都当"已跑"，close 落地后永不重试 → registry 里 S-3/星舰行全空两天。修：① `db/watchlist_automation.automation_applied_on(day)`（`skipped=FALSE AND applied_at IS NOT NULL`）为唯一"完成"判据；② `catchup_missed_eod_chain` 用它替换 `already()`，重试成功后**强制重跑 paper_s3_intake**（17:42 那次用的是盘前分数）；③ `paper_chain_watchdog._run_ok("watchlist_automation")` 同源（异常时才回退 sync 记录）。测试：db 层 2 + scheduler 4（skipped 重试、17:35 前不重试、watchdog 判据、既有 catch-up 用例全保）。**需重启后端生效** | — |
| OPT-221 | [x] 2026-09-17 · **卫星/星舰上下文加载优化（工程 P0-1 第一刀，逐位 parity）**：① `_load_rows` 改 `COPY TO STDOUT` + `set_types`（日期正则校验后内联窗口；行无序 → 本地按日期排序 + 按 ts_code 重建顺序，保住下游 stable-sort tie-break）→ 1.68M 行 10.4s→8.1s，逐票 diff=0；② habit 上下文只载 14:30/15:00 两个 5min 时点（`HABIT_CTX_TIMES`；原 7 时点里其余 5 个服务内无人读）→ `_load_bar5_closes` 7.6M→2.2M 行；③ calendar witness 快路径：官方 SSE 日历守门 + 三只见证票（000001.SZ/600000.SH/600519.SH）PK 索引日期（~10ms），见证缺口自动回退精确 DISTINCT（3.9s）——四个窗口 fast==slow 全部相等；④ **`daily(trade_date)` btree 回归**（反转 0009 的删除：当时只覆盖逐票前缀场景，漏了日期族）`MAX(trade_date)` 3.2s→**0.00s**、`count(*) WHERE trade_date>=X` 3.6s→0.07s；迁移 `0051_daily_trade_date_btree` + `db/daily.py` CREATE_SQL + 测试同步（BRIN 共存）。**效果**：pool compute 46→28s、habit timeline 42→27s、live panel 33→16s（`/backtest/satellite-signals/live`）。剩余大头：`daily` 行传输+建 dict ~8s、`bar_5min` 两查 ~9s、replay 特征 ~8s。测试：全量 **4345 passed / 3 skipped**、DB 基线零变化、ruff clean；新增非 ISO 窗口拒绝测试 + fast/slow calendar parity。**未做**（后续）：raw ctx 缓存（1.2GB 内存，需列式存储才有意义）、bar_5min 预聚合 amp_1430、timeline warmup 扩 5 档 | — |
| OPT-222 | [x] 2026-09-17 · **星舰 14:30 现场面板（用户：watchlist 要显示"今天该买什么，假如开闸"）**：① **14:30 job** `satellite_live_panel`（weekdays `30 14 * * 1-5`）：先抓全市场实时报价（`fetch_realtime_quotes_batched`，~6s，及时性优先）→ 载入冻结 habit ctx → 把报价合成当日行/14:30 print/15:00 mark（`_inject_day`，支持 mv/px/HL override）→ `satellite_signals_for_day` 出面板（与 paper/回测同源）→ 只有完整面板（decisionAvailable + 覆盖率≥50%）才原子落 `satellite_live_panel_latest.json`，否则只记 job 失败、旧快照不动；无启动 catch-up（错过就显示"待 14:30 判定"，避免抓错窗口）；② **API** `GET /api/backtest/satellite-signals/live-panel`（返回 `{panel, stale}`）+ shared Zod `SatelliteLivePanel*` + 前端 `useSatelliteLivePanelQuery`；③ **卡片** `SatelliteLegBlock`：今日快照优先 → 大号红/绿闸横幅（**闸 关 · 今日只卖不买** / 闸 开 · 今日 14:30 可补仓）+ 广度/跳空/桶/池 + 名单（关闸时也显示"若开闸，今日本会买"）+ 跳过原因；退出锚点改为 `exitDue == 今天`；无快照回退 replay 并把"补仓"改为"待今日 14:30 现场面板"；④ **口径**：mv 用昨收（实时报价无市值）、amp 用抓取时刻的 session high/low（14:30 定点即冻结口径）、与 paper/回测同一 `satellite_signals_for_day`；⑤ **测试**：后端 10（注入/覆盖/落盘纪律/任务/`requires_postgres` 历史日 parity：合成路径 == replay 面板含 gate/breadth/ranked/wouldFill）、前端 4（闸关/闸开/陈旧回退/卡片接线）、scheduler job 清单同步；全量后端 **4355 passed / 3 skipped**、DB 基线零变化、ruff clean；前端 **893 passed / 1 skipped** + tsc 0 error。**需重启后端生效**（重启后卡片读今日快照；今日 15:05 已手工跑过一份快照可先看效果）。**同日 UI 打磨**：闸徽标改为半透明描边胶囊（红/绿/琥珀/灰，10px，和全站 `xxx/15` 风格一致）；闸状态时间感知——平日 <14:40 无快照=「闸 待 14:30 判定」、≥14:40 无快照=「闸 快照缺失」（提示检查 14:30 任务，不再一直挂"待定"）、周末=「闸 今日休市」、有今日快照=「闸 开/关 · 今日 14:30」；回放日闸不再出现在头部（消除"待定 vs 开"并存），只以 `回放 {date}（非今日）` 对照；前端测试 895 passed / 1 skipped + tsc clean。**再次打磨（用户：提示要足够清晰）**：退出条目追加明确资金去向——星舰「→ 卖出资金停入 H2 停车腿（原油 513350.SH）」、星港/双子星「→ 卖出资金回到母港腿/港湾腿（该腿闲置现金停 H2 停车腿：…）」；无到期腿时显示「今日无到期卖出（继续持有 …）」；停车条目标注「卫星卖出款/空槽现金停这里；换仓与 trail 由 H2 收盘口径决定（本卡不预测）」；核心腿条目按 `stockGateClosed`（来自 portfolio health）写明「今日闸门关闭 → 不新开股票」+「停车腿 HOLD 原油 513350.SH」 | — |
| OPT-223 | [x] 2026-09-17 · **策略动作三通道提示（用户：组件 + Bark + notification，按所选策略）**：① **策略选择落库**：新表 `app_settings`（迁移 `0052`）+ `GET/PUT /settings/strategy-mode`（`LocalOriginGuard` 保护）；前端 `setStrategyMode`/WatchlistPage 挂载时镜像 PUT（fire-and-forget，后端坏也不影响 UI），Bark/通知文案据此生成；② **面板自带到期腿**：`build_live_panel` 先跑 pre-injection replay 取 `exits/heldLegs`（today 无 daily 行 → 到期腿保留），不再依赖 registry 是否维护；③ **动作合成单源** `service/satellite_actions.py`（compose：卖出/资金去向/闸开补仓或若开闸名单/停车 H2），Bark 与站内通知同源；④ **Bark**：新事件 `satellite_action`（注册 `KNOWN_EVENT_TYPES` + `format_bark` 标题 `🛰 {策略} · yyyy.mm.dd 14:30`）；14:30 job 落盘后按 `selected_strategy_mode()` 推送（非卫星档不推、dedupe/day+mode），并把既有 Bark 订阅（id 280）event_types 加入 `satellite_action` → **实测推送 `status=sent`**；⑤ **站内通知**：`build_notifications(mode)` 增加 `satellite_action` 项（lane=trade、anchor=`satellite-leg`、exit/开闸 high）；`GET /api/notifications?mode=` 透传 + 前端 `useNotificationsQuery` 跟随 `useStrategyMode`（切换策略即刷新）+ NotificationHub `ANCHOR_LABEL`；⑥ 卡片：`id="satellite-leg"`、优先用面板 `exits/heldLegs`。测试：后端 +14（settings db/route、emit_action、format_bark、通知项、route mode 透传）、前端 +5（mode 镜像、feed mode、面板到期腿、anchor）；全量后端 **4365 passed / 3 skipped**、DB 基线零变化、ruff clean；前端 **900 passed / 1 skipped** + tsc 0 error。**需重启后端生效**（重启后 14:30 自动推；今日已手工验证一条） | — |
| OPT-224 | [x] 2026-09-17 · **14:30 闸的样本偏差修复（用户：回测里 09-17 为什么"还持有仓位"）**：症状——Timeline `strategy=starship` 09-17 行「卫星 4仓 · 成交 4」= 卖掉 4 只到期腿后又买入 4 只；真因——`bar_5min` 自 09-11 起按设计只存「当日缺口股 + paper 持仓」（20–50 只/日），回放的 R-wide 闸 `_breadth_at_1430` 只看"有 print 的名字"→ 29/36 = **80.6% 判开闸**（真实全市场 27.9% = 关闸，14:30 现场面板同源）→ 09-11 起多日幽灵开闸/幽灵买入；另持仓腿当日无 14:30 print → 到期卖出按 qfq 收盘成交（基期混用）。修：① **闸加覆盖率守门** `MIN_BREADTH_COVERAGE=0.2`（`tot < 0.2 × 当日有行的票数` → 返回 None → 回退全市场收盘广度 = 既有历史 parity 路径，绝不采信薄样本）；② **现场面板快照落库** `satellite_live.persist_quotes`：14:30 job 把全市场报价（~5014 只）写进 `bar_5min`（`trade_time='1430'`、`source='live_1430'`、**只写 close**——快照 OHLC 是全天值，写进去会污染 `amp_1430`）；③ **源优先级**（`db/bar_5min` 冲突 WHERE 加 rank）：真实 5min bar (`baostock`/`tushare`/`ext_*`) > `ext_15min` > `live_1430` —— baostock 可覆盖快照，快照永不覆盖真 bar（保住 OHLC 与 14:30 成交价）；④ **派生 15:00 mark** `bar_5min.derived_1500_marks`：18:40 job 用 `daily` 的 qfq close × `adj_latest/adj` 还原**全市场 raw 收盘**（insert-only、排除 `.HK`）→ 近月回放的 prior MA20 与到期 mark 不再混 qfq；一次性补 09-03/09-11/09-15/16/17 ≈27.8k 行。**验证**：09-17 行 → `闸关 · 0仓 · 0成交`（4 笔到期卖出照常执行）；09-11 起的幽灵买入全部消失；`refresh_satellite_pool(2026-09-17)` 用修复代码重跑 → **removed=4**（registry 里 4 只幽灵卫星腿已清除）。**回测同问排查**：冻结窗（OOS2/train/valid/long）与全部 `compare_sat_*`/`diag_*` 脚本窗口 ≤2026-08-28/09-03 → **不受影响**；受影响面 = Timeline trailing/holdout、18:40 pool refresh（09-11 起）、`/satellite-signals/live`（DB print 路径）、eval 报告 holdout 行（描述性，非裁决）；S-3/港湾/停车/paper 账本不读 `bar_5min` → 免疫。测试 +9（闸守门×2、快照落库×2、`_quotes` 不落盘、派生 mark×2、job 落库、DB 源优先级）。**需重启后端生效** | — |
| OPT-225 | [x] 2026-09-18 · **停车统一到 H2（H-H2-UNIFY · 用户拍板）**：ETF 闲置停车收敛为唯一实现 `harbor.parking_replay(hyst_band=HYST_BAND=0.02)`——Live/paper/Timeline/watchlist/recon 全走它；新增 `HYST_BAND`/`held_mom`，`build_harbor_timeline` 传产品 band，`multi_asset_sleeve` 换仓加 2pt 迟滞闸（`hystBlocked`），`state_bucket_track.apply_parked_display` 统一，`parking_sleeve.hysteresis_parking_replay` 降为薄委托（删重复循环）。`parking_replay` 默认 0.0 保留 canonical 供冻结研究复现。依据：多窗综合平局偏 H2 + 换手 −52%。验证：`verify_harbor_live_vs_backtest` 473/473=100%、全量 pytest 4378 绿、DB 零变化、ruff clean | 档 [harbor-h2-unify](backtests/stable/harbor-h2-unify-2026-09-18.md) · 预注册 [prereg](designs/harbor-h2-unify-prereg-2026-09-18.md) |
| OPT-226 | [x] 2026-09-18 · **停车统一后的冗余清理**（行为不变，纯收敛）：① `?strategy=harbor_h2` 验证线退役——删后端路由分支 + `parking_sleeve.blend_harbor_h2_timeline`、`strategyCatalog` enum 值、前端 TimelineStrategy/标签/映射/subtitle、后端 contract 测试用例；② `harbor_h2_shadow` 退役——删 `service/harbor_h2_shadow.py` + `scheduler/harbor_h2_shadow_job.py` + 调度注册 + EOD catch-up + `SYNC_JOB_TYPES` + `/api/backtest/harbor-h2-shadow/latest` + shared `harborH2Shadow.ts`/`scheduler` catalog 条目 + 前端 `H2ShadowFollowCard`/query hook + `test_harbor_h2_shadow.py`；③ 旧 T6 退役见 OPT-179。验证：后端全量 4345 绿、shared 87 绿、前端 typecheck + ai-service typecheck + lint 0 error；前端 2 例失败为 **HEAD 既有**（TodayTodoCard/PortfolioHealthCard，与本次无关，已 git stash 复现） | — |
| OPT-227 | [ ] | **五策略记分卡按 H2 重跑**：OPT-226 只做代码收敛，记分卡「港湾」行与 `eval_etf_parking_baseline` 的 P1 臂仍是 canonical 参考值；需显式切 `hyst_band=HYST_BAND` 重跑（`eval_etf_parking_baseline` P1_H2 已是 H2，可直接作产品口径）并更新 `five-strategy-v2-scorecard` 与 stable 档 | — |
| OPT-228 | [ ] | **卫星腿 forward paper 账本 + 单组件操作台（OPT-186 slice 2a）**：① 后端 `service/satellite_paper.py`（只读投影）——`build_satellite_paper()` 用 `build_sgap_timeline(**HABIT_RECIPE, times=HABIT_CTX_TIMES)` 从 `SATELLITE_PAPER_INCEPTION`（2026-09-18，五档冻结次日）起回放，输出 `prereq{closedCount,target=20,met}` + `stats` + `closed[]`（blotter kind=fill，新→旧）+ `openLegs[]`；纯函数 `satellite_paper_from_sat()` 可注入 replay 输出做单测。② API `GET /api/backtest/satellite-paper`（只读、无 DB 写、口径与 timeline 同源）。③ shared `schemas/satellitePaper.ts` Zod + 导出 + 单测。④ 前端 `useSatellitePaperQuery` + `SatelliteLegBlock` 增「paper 账本」区（前置进度 closed/20 + 当前持仓 + 近期平仓 P&L + paper 收益/回撤），操作台单组件同时承载现场操作提示与账本。**不做**：写 `paper_trades`（避免污染 S-3 统计/对账）、`user_trades` satellite 腿 + recon（slice 2b，需迁移与对账链）。验收：后端单测（纯投影）+ shared 单测 + 前端组件用例；与 replay 同源回归 | — |
| OPT-229 | [x] 2026-09-21 · **交易成本严格化（真实世界口径 · 用户拍板）**：`service/paper_cost_model.py` 唯一源升级——① CN 佣金 万2.5→**万3** + 过户费 0.1bp/边 + 经手/证管费 0.541bp/边；② HK 补微费（SFC+HKEX+AFRC+CCASS 1.27bp/边）；③ 新增 **1-tick 滑点下限** `max(base_bps, tick/price)`（CN tick 0.01；HK 按 HKEX 价带表），低价股更贵；④ 最低佣金（CN ¥5、HK$100）按 `REFERENCE_CAPITAL=¥1M × 10% 槽位` 折算（该规模不触发，残差声明）。引擎 `backtest_engine` per-trade + NAV 双通道改价格自适应（`explicit_entry/exit_frac` + `slippage_frac(price)`）；Live `paper_trading`/`paper_s3` 同步；`state_bucket_track` 卫星仍静态单源；`user_trades_stats` 派生不再硬编码 0.3。CN 静态 30→**32.28bp**、HK 90→**92.54bp**。测试：`test_paper_cost_model` 重写 + 8 测试文件 expected 同步，全绿；ruff clean。**2026-09-21 已重固化 + 重跑**：CN `walk_forward_baseline.json` sha `545d385c7339` tag `s3-cn-baseline-20260921`（+37.3/+39.0/+38.7，long +81.2，Δ −0.7/−0.1/−0.1）；HK sha `88c21fb48d20` tag `s3-hk-baseline-20260921`（+17.4/−0.7/+63.9，Δ −4.3/−0.5/−1.8）。五策略重跑（新数见 [scorecard §9](backtests/stable/five-strategy-v2-scorecard-2026-09-16.md)）：港湾 P1 +54.4/+52.0/+50.2/long+197.1；母港 M50 +36.2/+34.9/+25.6/long+115.1 PASS；星港 w0.2 +64.8/+38.0/+20.9/long+163.3 PASS chosen 0.2；双子星 +144.6/+52.2/+24.5（valid Δ−25.7 REJECT）；星舰 A2_true +239.2/+65.3/−0.3/long+913.2。**⚠️ 新成本下裁决变化**：港湾 **P1_H2 被 HK3 MDD 门拒**（longMDD −24.4 vs canonical −22.9；产品=H2，需用户复核）；星舰 valid Δ−6.8 破 K1；卫星/双子星 valid 弱（部分为 `bar_5min` 数据漂移）。**附带修复**：`backtest_engine` `next_1430` 原始价→qfq 换算反了（`raw×ratio` 应为 `raw/ratio`；实测 raw/qfq==adj_latest/adj）→ 已修；该实验变体（s3-clock-1430 REJECT）若要重开需重跑 | — |
| OPT-230 | [x] 2026-09-21 · **双子星整体复活为五档正式并行档（用户拍板）**：`strategy_catalog` twin_star 行 `status=parallel_candidate`→`product_candidate`、`statusLabel`→`并行 · 正式`、`roleLabel`→`均衡`、structure/cons 去掉"只作对照/不进实盘"负向措辞；**全五档 windows 刷成 2026-09-21 新成本数**（UPDATED=2026-09-21）：港湾 54.4/52.0/50.2/197.1 · 母港M30 43.4/41.9/35.4/146.6 · 星港0.2 64.8/38.0/20.9/163.3 · 双子星 144.6/52.2/24.5/299.8 · 星舰 239.2/65.3/−0.3/913.2；前端 `strategy-settings`（tag/描述）、`BacktestPage` 三处 twin_star 文案 + 星舰 long 数字同步；twin-star stable 档加 §4b addendum；测试 `test_strategy_catalog`（role/updated/数值）同步。**展示/研究口径，Live 仍=港湾**（进 Live 须另起预注册）。后端 23 + 前端 35 + shared 6 全绿、ruff clean | — |
| OPT-231 | [ ] | **星舰稳健版（闲置现金停 B3 风险预算）· 候选登记 + 可选接线**：H-SAT-IDLE-B3-R 回撤口径 **PASS**——`A4_true`（卫星 + 闲置现金停 B3 逆波动率 5 资产月频、`w_t=cashShare(T−1)` 因果、5bp/边）long **+654.3% / CAGR 52.1% / MDD −6.8% / SR 3.67** vs 现行套筒 A2_true +913.2/61.6/−29.9/2.11（让 259pt 收益换 23.1pt 回撤、SR +1.56）；中间档 A6_true（idle 50% 套筒 + 50% REPO）long +681.4/53.2/−15.9/2.88。**已登记文档**：`strategy-recipes.md` §5b + §6 裁决表行、`backtests/stable/sat-idle-b3-{,-drawdown}-2026-09-21.md`、`SUMMARY.md`、预注册 `designs/sat-idle-b3{,-drawdown}-prereg-2026-09-21.md`；代码 `scripts/eval_sat_idle_b3.py --premise {returns,drawdown}`。**待办（需用户拍板）**：① 可选 Timeline/目录展示接线（纯显示层，仿星港/星舰 overlay）；② 若进 Live 须另起 Live 口径预注册 + paper 前瞻。**不进 Live**；星舰前置不变。**附带 REJECT（H-SAT-IDLE-B3-MENU，同日）**：B3 菜单补 OIL/恒生科技/有色**全面变差**（B3(6) long +45.6/−5.3/1.60 vs B3(5) +46.2/−4.7/1.76；三窗 Δ vs A4 `[−7.3,−3.3,−0.5]`）→ **5 资产菜单是平台，补腿=稀释**；见 [sat-idle-b3-menu-2026-09-21](backtests/stable/sat-idle-b3-menu-2026-09-21.md) | — |
| OPT-232 | [x] 2026-09-21 · **星舰稳健版（a25）并列展示接线（用户拍板）**：H-SAT-A25 **PASS**（三窗 Δ vs A4_true `[+1.5,+3.9,+0.3]`、long +74.1；a25 long **+728.4%/CAGR 55.0/MDD −6.9/SR 3.49/Calmar 7.98**）→ 与激进版星舰**并列**。改动：① `state_bucket_track.apply_parked_display(parked_mode="a25")` + `build_state_bucket_timeline(parked_mode=)`——停车腿按 `0.25×套筒净收益 + 0.75×B3 日收益` 混合（B3 = `homeport.risk_budget_nav`）；② 路由 `strategy=starship_robust`（habit 卫星 + a25 停车，`mode/strategy/satWeight/baseKey`）；③ `strategy_catalog.py` 新增 `starship_robust` 行（紧随星舰，`product_candidate`/「稳健 · 候选」/`balanced`/regime 三块）；④ shared `strategyCatalog` key + timelineStrategy 枚举；⑤ 前端 `TimelineStrategy`/`TIMELINE_STRATEGY_LABEL` + `BacktestPage` 三处 record。验证：a25 Timeline 实跑 OK（2025-08~2026-08 fused +71.85/MDD −10.7）；后端 catalog 6 绿、ruff clean；前端 typecheck 0 error + BacktestPage/Catalog/Panel/settings/health 63 绿；shared 6 绿。**展示/回测口径，Live 仍=港湾** | 档 [sat-a25-robust](backtests/stable/sat-a25-robust-2026-09-21.md) · 预注册 [prereg](designs/sat-a25-robust-prereg-2026-09-21.md) |
| OPT-233 | [x] 2026-09-21 · **scheduler cron 星期 off-by-one（周一任务全漏跑）**：根因 = 全部 cron 按**标准 cron（0=周日）**写，但 `CronTrigger.from_crontab` 的 `day_of_week` 是 **0=周一** → `1-5` 被解读为**周二~周六**、`1`/`5` 被解读为**周二**/**周六**。后果：**所有"工作日"job 周一永久漏跑**（EOD 类靠补跑链自愈；**盘中时点类无补跑 → 14:30 卫星面板周一永久缺失**，2026-09-21 实锤：`sync_job_record` 无当日记录、面板文件停在 09-18、卡片"待 14:30 判定"）。修复：**36 个 scheduler 文件的星期字段全改**为**无歧义英文名**（`1-5`→`mon-fri`、`5`→`fri`、`1`→`mon`、`2-6`→`tue-sat`、`1-7 * 1`→`1-7 * mon`），其余字段不动；新增 `tests/test_scheduler_cron_weekdays.py`（14:30 周一必发；≥20 条 `mon-fri` 触发器周一覆盖+周六跳过；禁止数字星期区间的守卫）。**14:30 面板加粗**：`satellite_live_job` cron → `30,33,37 14 * * mon-fri`（**14:30 主抓 + 14:33/14:37 盘中重试**），`run()` 改**幂等**（当日面板已落盘 → `already_captured` 跳过，健康日重试零动作；失败在盘中重抓；**收盘后不跑、无跨日补跑**）；`test_satellite_live` 加幂等用例并把 `test_incomplete_panel_records_failure` 隔离到 tmp 面板路径。验证：`test_satellite_live`/`test_scheduler_cron_weekdays`/`test_scheduler_jobs_extra`（+4 个）**267 passed / 2 skipped**；ruff clean。**需重启后端生效**（已重启，catalog 已出 6 行） | — |
| OPT-234 | [x] 2026-09-21 · **卫星卡开闸时可直接买（竖排 + 100 整数倍股数 + 买入按钮）**：`SatelliteLegBlock` 开闸时把"今日买入"从横排改成**竖排每只一行**，按 `slotLotShares`（等权 25% 槽、**不套 15% 集中度上限**、不套排名加成）把**总资金×25%÷价**折算成**合规整数手**（沪深 100×、科创 200+1、ETF 100 份），显示"买 N 股 ≈ ¥X"；每行一个**买入**按钮（`recordUserTrade` → `POST /trades`，`leg='satellite'`、`positionPct=25`、价用面板 14:30 print、`source=RESEARCH`），成功后按钮变"已记录"并刷新 `userTrades`。**总资金不再在此行内设**：新增共享 **「账户」区** `components/account/AccountBar.tsx`（**只填总资金**——港币汇率不再露脸，港股按固定 0.92 估算；onBlur 存 `account-settings` localStorage、随时改、全局共享；样式=半透明圆角卡 `bg-[var(--k-surface)]/55 + backdrop-blur-md`），挂到 **Watchlist 页顶部**（`StrategyModeBar` 下），Settings 页原 `AccountSettingsPanel` 保留（高级项）；卫星卡未设资金时只提示"在上方「账户」里填"。**股票名 + 雪球链接（2026-09-21 同日）**：面板 ranked 增 `name`（`satellite_signals._names_for` 查 `stock_basic`，best-effort）+ `satellite_live.build_live_panel` re-map 保留 `name`（曾漏→补）；shared `SatelliteLivePanelNameSchema`/`SatelliteSignalNameSchema` 加 `name`；卡片每行把 `代码 名称` 做成 **`https://xueqiu.com/S/{SH|SZ}{code}` 新窗口链接**，便于快速核验。配套：新增 `satellite` leg —— shared `UserTradeLegSchema`、后端 `LEG_SATELLITE`+`CREATE_SQL` CHECK、**Alembic `0053_user_trades_leg_satellite`**（已 upgrade）；`lot-sizing.slotLotShares` 纯函数。测试：`lot-sizing` 18 + `SatelliteLegBlock` 16 + `AccountBar` 2 · shared `userTrades` 10 · 后端 `test_user_trades_db`（satellite 往返）+ routes 15；前端全量 **903 passed**/typecheck 0 error/eslint 0 error；ruff clean；端到端 POST `leg=satellite` ✅ 且测试行已清理（count 41→41）。**展示/记账辅助，Live 仍=港湾** | — |
| OPT-235 | [x] 2026-09-22 · **稳健星舰（a25）加进策略选择器、挨着「星舰」，并设为默认档**：用户 2026-09-22 决策（原油停车回撤触发）——`StrategyMode` 增 `'starship_robust'`，`STRATEGY_MODES` 排在 `starship` **之后**，`DEFAULT_STRATEGY_MODE` 由 `starship` 改 `starship_robust`（**全应用默认**：Watchlist `StrategyModeBar`、Settings `StrategySettingsPanel`、Timeline 默认档）；label「稳健星舰」、tag「稳健 · 主用」、desc = a25（25% ETF / 75% B3、long +728.4 / MDD −6.9 / SR 3.49）。**穿透接线**：`PortfolioHealthCard`（`coreView` 排除、`satelliteMode` 收敛 4 处 `mode===` 判断、`satStrategy` 直传 `starship_robust`、B3 腿也显示）、`PickStrongAlignBanner`（文案「卫星 standalone + 闲钱 25% ETF / 75% B3」）、`SatelliteLegBlock`（`ResearchStrategy` 增 `starship_robust` + `RESEARCH_STRUCTURE` 条目；`isStarship`/`isRobust` 收敛 4 处判断；停车行对稳健版改成**准确的 a25 口径**——「ETF 腿持有 OIL · 闲钱占比 X% · 另 75% 停 B3（5 资产逆波动率，月初再平衡）」，不再误写「停车权重 100%」）。后端 `settings_routes.py`：`STRATEGY_MODES` 增 `starship_robust`、`DEFAULT_STRATEGY_MODE` 改 `starship_robust`（notification 文案跟随）；`app_settings.strategy_mode` 已置 `starship_robust`。**注意**：仍为展示/推送口径，Live 下单恒为港湾；星舰稳健版进 Live 前置 = paper 3/20 + 风险授权（未满）。测试：`strategy-settings` / `StrategyModeBar`（默认选中 + 顺序）/ `SatelliteLegBlock`（+a25 用例）更新；前端全量 **904 passed**/typecheck 0 error/eslint 0 error；后端 `test_settings` 4 passed、ruff clean；`GET/PUT /settings/strategy-mode` 实测 ✅、`timeline?strategy=starship_robust` ✅（末行 parkedTs=513350.SH）。**同日补漏**：`satellite_actions.SATELLITE_MODES` 未含 `starship_robust` → 14:30 的 Bark/站内推送会被 `mode=starship_robust has no satellite leg — no action push` 静默跳过（卡片不受影响）。修：`STRATEGY_LABELS` 加「稳健星舰 a25」、`SATELLITE_MODES` 加该模式、`_STANDALONE_MODES` 收敛 proceeds 判断、停车行对稳健版写明「25% 停 ETF + 75% 停 B3」；`test_notifications` 加稳健版用例（22 passed）。**需重启后端生效**（dev 无 --reload，已重启；scheduler 14:30 job 照常） | — |
| OPT-236 | [x] 2026-09-22 · **a25「目标配置」卡（把三条腿放在同一 100% 尺度上）+ 隐藏误导的港湾日对齐 banner + B3 raw px**：用户反馈「不知道 a25 该持有什么、腿多不好操作」。新增 `A25TargetBlock`（`starship_robust` 下渲染）——用 `useAccountSettings` 总资金 + `useB3StateQuery` 权重，把 **卫星腿（按今日面板 wouldFill+heldLegs 计 4 槽占比）/ 停车 ETF 腿（空槽×25%）/ 停车 B3（空槽×75%，5 资产逐行）** 列成一张表，每行给 **占全仓 % + 金额 + 整数份数**（`slotLotShares`，ETF 100 份）+ 合计 100%；空仓日自动变成 25%/75% 全仓口径。**隐藏 `PickStrongAlignBanner`**（`starship`/`starship_robust`）：它按**港湾核心腿（目标 100% ETF）**对账，对 standalone 档只会误报「闲置偏多、应买入 ETF」——正是用户困惑的来源。**B3 raw px**：`strategy_today.b3_state` 增 `_raw_last_closes()`（查 `daily` raw，ETF `adj_factor` 为 NULL）+ universe 每项 `px`（**下单必须用可交易 raw 价，不能用复权引擎价**：510300 引擎 5.84 vs raw 4.61），shared `B3WeightSchema` 加 `px`；note 去掉写死的「占组合 50%」改成「占组合比例由档决定（母港/星港 30%、a25 = 空槽×75%）」。测试：`A25TargetBlock` 2（1 槽 / 空仓两态）+ 前端全量 **906 passed**/typecheck 0 error/eslint 0 error（8 既有 warning）；后端 `test_strategy_today`+`test_settings` 7 passed、ruff clean；`b3-state` 实测返回 raw px（510300 4.608 / 511260 134.757）。**展示/执行辅助；Live 仍=港湾** | — |
| OPT-237 | [x] 2026-09-22 · **一键记账：目标配置卡每行「记录」→ 同时写 watchlist + trade journal（用户核心诉求=记录，不是显示）**：用户「我点一下就记录下来了吗？我的核心是记录而不是显示」「同时记录到 watchlist，我账户已经操作了，同步非常麻烦」。`A25TargetBlock` 每行（停车 ETF 腿 + B3 5 资产）加**记录按钮** → `upsertWatchlistOpenTrade(loadWatchlist(), …)` + `saveWatchlist(next)`（**持仓进 watchlist**，驱动 holdings/recon）+ `recordUserTrade({leg:'parking', source:'RESEARCH', symbol:'ETF:<code>', positionPct: 目标占比%})`；**已记录态跨刷新**（用 `useUserTradesListQuery` 预标记当日同 symbol，不靠 session state）。**卫星卡买入键同样补 watchlist 写入**（`leg:'satellite'`，`source:'satellite'`），与 S-3 `confirmBuy` 三处一致。符号约定沿用套筒 `ETF:<code>`（`multi_asset_sleeve` 同源）；shared `WatchlistItemSchema.symbol` 是自由字符串、`source` 用 `research`（无 parking 枚举值，不扩 schema）。测试：`A25TargetBlock` 3（+记账断言 watchlist+journal）/ `SatelliteLegBlock` 17 + 前端全量 **907 passed**/typecheck 0 error/eslint 0 error；后端实测 `POST /trades {leg:'parking', symbol:'ETF:513350'}` ✅ 并已删除测试行（用户 8 条 `ETF:513350` 为真实 MANUAL 记录，含今日卖 OIL 81%，**未触碰**）。**同日再收敛为「一个操作区」（用户：操作区放同一位置、全绿、策略对用户隔离）**：`A25TargetBlock` 重写成**扁平下单清单**——不再分「卫星腿/停车 ETF/B3」，把**卫星买入（面板 wouldFill，`CN:<code>`，25%）+ 停车 ETF 腿 + B3 5 资产**排成同一列，每行 = 雪球链接 + 价 + 「买 N 股/份 ≈ ¥X」+ **同一位置的绿色圆角「买入」键**（`ml-auto rounded-full border-emerald-600/40 text-emerald-700`，与卫星行同款），字号 10px→**text-xs**；卫星行带 `+gap% · amp%` 注。卫星卡加 `hideBuyRows`（a25 视图下不再重复买入行，改为「买入见上方今日下单清单」）。记账统一：每行 → `upsertWatchlistOpenTrade`+`saveWatchlist`（watchlist）+ `recordUserTrade(leg=parking|satellite)`（**账本=user_trades**）。测试：`A25TargetBlock` 4（扁平/卫星记账/停车记账/空仓）+ `SatelliteLegBlock` 17 + 前端全量 **908 passed**/typecheck 0 error/eslint 0 error。**同日再补（用户：美化/背景色、加占总资金 %）**：整块加**卡片背景**（`rounded-xl border-sky-500/25 bg-[var(--k-surface)]/55 p-2 shadow-sm backdrop-blur-md`，行改 `bg-[var(--k-bg)]/40` hover 天蓝）；每行**加「占总资金 %」**（`25% / 18.75% / 48.66%`，天蓝加粗）；**停车 ETF 兜底**——timeline 抖动导致 `parkedHeld` 为空时（用户实见「卫星腿：最近交易日状态不可用」+ OIL 行消失）改用 `portfolio-health.multiAssetSleeve.pick`（`sleevePick` prop，价取 `close`），OIL 行不再丢；`etf` 用 `useMemo` 修 exhaustive-deps。测试：`A25TargetBlock` **5**（+%断言 +sleevePick 兜底）/前端全量 **909 passed**。**同日自愈补丁（用户：检查我操作是否进账本）**：核对 09-22 —— 账本 7 笔全在（`CN:603019` satellite RESEARCH 25%、手动 `ETF:513350` SELL s3 81%、`ETF:510300/510500/518880/513100/511260` parking RESEARCH 5 笔）；watchlist 5 只 ETF 已同步（source=research、entry 09-22、带 cost/pos），**但 `CN:603019` 只在账本、不在 watchlist**（14:34 点击早于 watchlist 写入上线）。修：`done = 账本 AND watchlist`，**只缺 watchlist 的行仍可点** → 点击只补 watchlist、**不重复写账本**（`if (!recordedToday.has(sym))` 守卫）；测试 +1（journal-only 自愈不重复）/前端全量 **910 passed** | — |
| OPT-238 | [x] 2026-09-23 · **a25 面板到期日夹取 bug（幻影成交/幻影空仓的根因）+ 组件改用冻结 timeline + 「持有」视图 + 0 点结转**：用户「第二天没开盘，组件还显示昨天的买入，我期望昨天的买入结转成持有，然后告诉我今天怎么操作」。**确认 3 件事**：① 组件显示的是**上一交易日 14:30 的面板**（今日 14:30 未到）；② 「买入」重现是因为「已记录」按 **today** 判定（`tradeDate === today`）——新的一天自然 0 笔；③ OIL 行消失（`parkedHeld` + `sleevePick` 皆空）。**更严重（根因）**：`build_live_panel` 用 `end=day` 跑回放，而回放的日历只含**已有 `daily` 行的日子**（面板日盘后才有当日行）→ 在飞腿 `exitDue` 被夹到面板日。复现：`end=09-22 → exitDue 09-22`；`end=09-23 → exitDue 09-23`。后果：09-22 面板把 4 只在飞腿（09-21 进、body 3、真到期 09-23）全判成「今日到期」、`heldLegs=[]` → 组件以为只占 1/4 仓、空闲 75% → **幻影下单**（OIL 18.75% + 5 只 B3 56.25%）+ 面板吐**幻影成交 603019**（冻结引擎 09-22 `filledToday=0`、4/4 满仓）；组件还**从不显示卖出**（那 4 只腿到期日根本没告知）。**修 ①（后端，根因）**：`trade_calendar_utils.nth_open_date_from(entry, body)`（按 **SSE 交易日历**取 entry 起第 body 个开市日，body = `heldDays + daysLeft` = 冻结 body 的数据日数）+ `satellite_live._recompute_exit_due/_due_key`（面板构建前就地重算 `exitDue`，日历不可读则保留原值；exits 判定改 `<= day`）。测试：`test_satellite_live` 21（+3 单元 +1 requires_postgres 断言 09-22 面板 `heldLegs` 非空且 `exitDue=2026-09-23`、`exits=[]`）。**修 ②（前端，组件重建）**：`A25TargetBlock` 的**持仓腿改读冻结 timeline `openPositions`**（`satQ` 已在查；面板 `heldLegs` 仅作兜底）——不再信面板的 held/exits 拆分；**买入按空槽上限截断**（`slice(0, capacity - stillHeld)`，`satCapacity` 从 timeline 取，不再硬编码 4）；**套筒/B3 规模改用引擎 `parkedHeld.weight`**（真实闲置 31.24% → 套筒 7.81%、B3 23.43%，不再用槽位反推）；**「已记录」按面板日**判定；**0 点结转**（用户拍板）：`panelDay === today` → 「今日下单」，否则 → 「**当前持有**」（成本/现价/盈亏/到期日；到期腿带「卖出」键，**未持有则不显示卖出**，避免幻影卖出）；套筒/B3 只在**未记账**时出现在下单清单（首次建仓），之后是持仓。`SatelliteLegBlock` 的 exits/held 拆分同步改为**优先 timeline `openPositions`**（面板仅兜底）。测试：`A25TargetBlock` **8**（扁平/满仓截断/记账/0 点结转/兜底/自愈/关闸/持有卖出）+ `SatelliteLegBlock` 17 + 前端全量 **912 passed**/typecheck 0 error/eslint 0 error；后端 `test_satellite_live` 21、ruff clean。**同日再补（用户：为什么提示我卖出？）**：`当前持有` 对 4 只腿（300220/300932/603137/001331）给了**卖出键——错的**。它们只在自选表里，是**卫星自动化镜像**的（`source='satellite'`、`positionPct/costPrice = null`），账本 0 条 → **用户从没买过**；「是否持有」误用了被镜像污染的自选表。修：新增 `holdsMap`（自选表里 **positionPct > 0** 才算持有）+ `userHolds(sym)`（holdsMap 或账本有条目）→ 未持有的到期腿显示「未持有」、**不给卖出键**、不写幻影卖出；**卖出份数改用记录的 positionPct**（不再用 25% 名义槽位）；**名称**取自自选表（金运激光/三友联众/恒尚节能/胜通能源，bith 之前重复显示代码）；去掉冗余的「到期」注。测试：`A25TargetBlock` **9**（+镜像不出卖键）/前端全量 **913 passed**/typecheck 0 error/eslint 0 error。**用户续：「未持有的就不显示了」** → `当前持有` 改为**只列用户真实记录的仓位**（`holdsMap`，positionPct > 0）：不再按策略 book 迭代（否则会列出只被镜像的腿）；持有的卫星腿用 `userHolds` 判定并带上策略的到期日（到期 → 卖出键）；非 book 的记录仓（套筒/B3/策略外）走 `tsMap`/`priceOf` 解析交易所与最新价（timeline book → b3 px → 套筒价 → 面板 ranked px1430）；名称/成本取自自选表。测试：`A25TargetBlock` **9**（滚动到持有=仅记录仓、镜像腿隐藏）/前端全量 **913 passed**。**用户续（比例对不上 + 缺一只股票）**：核对 registry —— 41 项里只有 6 项带仓位（合计 **75.19%**），缺 **`CN:603019` 中科曙光**（账本有 `2026-09-22 BUY 25% @84.77`，registry 没有 —— 那天记账早于「记账同步自选」上线）→ 补进 registry（GET→append→POST，42 项，合计 **100.19%**；备份 `/tmp/wl_backup.json`）。**同步洞（已修）**：根因在 `hydrateWatchlist` 的合并策略 —— `pendingSync`（本地有未推上去的改动，POST 失败/离线）时仍按「远端非空优先」合并 → **本地已卖的仓会被 registry 的旧值覆盖**（OIL 81% 卖出被还原成 19% 即此）。修：`mergeWatchlistRemoteWithLocal(remote, local, { preferLocal })` + `pick()`，`hydrateWatchlist` 在 `pendingSync` 时传 `preferLocal: true`（本地至少同样新）并强制回推；默认行为不变。测试：`watchlist-storage` **16**（+preferLocal 卖出胜出 / 默认远端优先 / 未推本地卖出存活并回推）+ 前端全量 **916 passed**。**用户续（股票显示卖出时间 + 其他需调整要提示）**：`当前持有` 行加 —— ① **卖出时间**：持有/到期的卫星腿显示 `到期 {exitDue} 14:30 卖出`（策略 14:30 出场口径）；不在 a25 book 的股票显示 `策略外 · 无策略到期`；② **权重偏离提示**：有策略目标的行走 `Row.target`（卫星腿 25%、套筒 `idle×0.25`、B3 `idle×0.75×内部%`）与实记 `positionPct` 对比，`|Δ|>1pt` 显示 `目标 X% · 偏多/偏少 Ypt`（>3pt 标黄）；③ 套筒/B3 行的 pnl 由 `px/cost` 现算；④ 底部 `⚠ 需调整：卫星腿 策略 N 只 / 你持 M 只；K 项权重偏离目标 >3pt`。测试：`A25TargetBlock` **10**（+卖出时间/目标偏离/策略外）/前端全量 **917 passed**/typecheck 0 error/eslint 0 error。**用户续（603019 是昨天的 s-gap 没被识别；偏多偏少要能操作）**：① 新增 `GET /api/backtest/satellite-signals/exit-due?entries=`（`nth_open_date_from(entry, BODY=3)`，shared `SatelliteExitDueResponseSchema` + `useSatelliteExitDueQuery`）—— 引擎没纳入的面板信号腿（603019 在 09-22 4/4 满仓时买的）也能算出 `到期 2026-09-24 14:30 卖出`，行标 `（面板信号）` 而非 `策略外`；② 偏离行加**调仓按钮**：`Row.rebalance={side,pct}`（卫星腿 25% / 套筒 / B3 目标 vs 实记），`|Δ|>1pt` 出「减仓/加仓 X%」按钮（红色/绿色），份数按 Δ 算，`record()` 统一取 `tradeOf(row)`（HOLD 行也能记）；账本日口径修正（BUY→面板日，SELL/调仓→今天）。测试：`A25TargetBlock` **12**（+面板信号到期 / 调仓卖出断言 `ETF:513350 SELL 11.19`）+ 后端 `test_satellite_live` **22**（+endpoint 用冻结 BODY）/前端全量 **919 passed**/typecheck 0 error/eslint 0 error/ruff clean；实测 `exit-due?entries=2026-09-22 → 2026-09-24`。**用户续（目标显示 0% / 阈值太敏感）**：① **`目标 0%` bug** —— 后端重启后的冷启动瞬间 timeline 查询没回来（`parkedHeld` 为 null）→ `idle=0` → 套筒/B3 目标全算成 0% → 每行都像"巨幅偏多"。修：`idle > 0` 才给套筒/B3 目标（无 idle → 不出目标、不出调仓键）；套筒/B3 行加 `tsMap` 兜底 `guessTs(sym)`（`ETF:513350`→`513350.SH`，`bareCode` 去前缀）+ `bareCode` 作名称兜底 → 不再显示 `ETF:513350` / `¥—`。② **阈值 1pt→3pt**（`DRIFT_PCT=3`）：用户「小于 3% 就不提示卖出了，有一点点偏差可以接受」——偏多/偏少文字与调仓键都只在 `|Δ|>3pt` 出现。测试：`A25TargetBlock` **13**（+无 idle 不显示 0% 目标）/前端全量 **920 passed**/typecheck 0 error/eslint 0 error。**用户续（两个减仓是今天的指令吗？那买哪个？）—— 发现目标基准错了**：a25 停的是**本书自己的闲置现金**（空槽 + 已实现盈利），不是**引擎那本的** `parkedHeld.weight`。引擎的 31.24% 是"4/4 满仓 + 一年累计盈利"的产物；拿它当实盘目标会让「持 1 只腿」的书去把套筒/B3 砍到 7.81%/23.43%，**合计只剩 56% → 44% 现金没停车，违反 a25 规则**（卫星 + 停车必须 = 100%）。修：`idle = max(0, 1 − Σ用户记录的卫星腿占比)`（`satUsedPct`）→ 用户 1 只腿 = 25% → idle 75% → 套筒 **18.75%** / B3 **56.25%**，与其记录（19%/56.14%）一致 → **不出减仓键**（09-22 那批 OIL/B3 金额本来就是对的，我上一轮"用引擎权重"的改法是错的，已回退）。测试：`A25TargetBlock` **13**（含"1 只腿的书不该被提示卖套筒/B3"回归）/前端全量 **920 passed**/typecheck 0 error/eslint 0 error。**注：OIL 19% 与 B3 56.14% 是用户真实记录值，未改写**（B3 相对 a25 目标 23.43% 是仓位偏差，属「目标−现状」对账范畴，不是数据错误）。**展示/执行辅助；Live 仍=港湾** | — |
| OPT-239 | [x] 2026-09-23 · **实盘账本接入 paper 前置（用户：「我一味 a25 稳健版 已经在统计了，帮我接一下」）**：`paper 20 笔` 前置原来只数**冻结 replay 投影**的平仓笔数（`/satellite-paper`），用户的真实成交（`user_trades leg='satellite'`）不计数。新增 **journal 来源的前向 book**：`satellite_paper.satellite_user_book_from_rows(rows, start, end)`（纯函数：按 symbol **FIFO** 匹配 BUY/ADD→SELL，部分卖出只平对应份额；净收益 = 未取整 gross − 静态 32.28bp；只计 `entryDate ≥ inception` 的**已平仓**，开仓列出不计数）+ `build_user_satellite_book()`（只读 `user_trades`）+ 路由 `GET /api/backtest/satellite-paper/user`（形状与 replay book 一致，可直接复用 shared `SatellitePaper`；replay book 保留作引擎对照）。前端：`useUserSatelliteBookQuery` + `SatelliteLegBlock` 新增 `userBook` prop → 显示「实盘账本（我的成交 · 自 2026-09-18）前置 N/20 · 已平仓/持仓 · 胜率 · 单笔均净」（`data-testid=satellite-user-book`），由 `PortfolioHealthCard` 接线。测试：后端 `test_satellite_paper` **7**（+FIFO 计数/部分卖出只平对应份额/剔 pre-inception 与脏行/空 book）、ruff clean；前端全量 **920 passed**/typecheck 0 error/eslint 0 error。实测 `GET /satellite-paper/user` → `source=user_journal, prereq 0/20, openLegs=[CN:603019 09-22]`（用户当前真实卫星仓）。**2026-09-23 追加**：`今日下单` 只列**引擎自己的腿**（4 笔 300220/300932/603137/001331 到期卖出，全标「未持有」），用户会找不到自己买的 4 只（用户原话「买的 4 个不在这里，显示了 4 个别的」）→ `实盘账本` 现在列出**我的持仓**（代码+名称+入场→到期+仓位，`data-testid=satellite-user-legs`，到期走 `useSatelliteExitDueQuery`，名称走 `loadWatchlist`）；`SatellitePaperOpenLegSchema` 加可选 `positionPct`。**2026-09-23 再追加**：`卫星腿` 组件在 timeline 行缺失时**整块 early-return 成一行「最近交易日状态不可用」**，把用户的实盘账本一起吞掉 → 改成 `userBookNode` **两个分支都渲染**；表头加「策略」标注 + `我的 N/4 槽`（`data-testid=satellite-my-slots`），区分策略账与自己的账（用户原话「这个组件还是显示在策略里已经买的内容」）。前端全量 **926 passed**。 | — |
| OPT-240 | [x] 2026-09-23 · **「没办法卖出」+ 浮点权重（用户报障）**：a25 的 sleeve/B3 目标乘出 `1.4625000000000001` 这类浮点，写进 watchlist 后 (a) `TradeActionDialog` 的卖出 % 预填值过不了 `/^\d+(\.\d{0,2})?$/` → **确认卖出按钮永久禁用**（用户点不动）；(b) watchlist 行内仓位格显示一长串小数。修复：① `normalizeWatchlistItems` 把 `positionPct` **四舍五入到 2 位**（每次 load/save 都跑 → 顺带治愈已存的脏值）；② `TradeActionDialog` 的 `heldPct` 预填/展示也取 2 位；③ `A25TargetBlock` 的 `sleevePct`/`b3TotalPct`/每只 B3 目标/落账的 `positionPct` 一律 `round2`。附带修 **停车场卖不掉**：`target` 的 `idle > 0` 守卫让卫星满槽（idle=0）时目标变 null（不是 0）→ 无减仓按钮；改成只要 sleeve/B3 命中就给 target（0 是合法态），且 `target === 0 && pct > 0` 时**无视 3pt 漂移阈值**一律给减仓（小权重 B3 行也能清）。另修 **空槽计数**：`freeSlots` 只减引擎账本里未到期的腿，漏了「用户持有但引擎没记」的腿（如 2026-09-22 满槽日买的 603019）→ 卡片多给一个买单；现按用户实际 CN 持仓扣槽。测试：`TradeActionDialog` **7**（+浮点预填可卖）、`watchlist-storage` **17**（+四舍五入）、`A25TargetBlock` **15**（+满槽清停车场 / +off-book 占槽）；前端全量 **923 passed**、tsc 0 error、eslint 0 error。 | — |
| OPT-241 | [x] 2026-09-23 · **「今日下单」改成以用户账本为准（用户拍板）**：原来 `A25TargetBlock` 的 `book` = 引擎冻结账（`openPositions`），而买入用**现场 14:30 面板** → 两套来源混在一起：卡片让用户卖 `300220/300932/603137/001331`（引擎 09-21 的腿，用户从没买过，全标「未持有」），同时让他买面板候选。实测同一天两套口径选出的名字根本不同（引擎回测 000428/000676/001216/002051 vs 现场面板 603125/300990/002982/301058）——因为引擎用**收盘/昨收**重放、现场用 14:30 真实成交价。改法：`book` = **用户自己的 CN 持仓**（watchlist `positionPct>0`），到期日走冻结规则（`useSatelliteExitDueQuery(entry)` = entry + BODY 个交易日）；`dueToday`/`stillHeld`/`freeSlots` 全按用户账；买入仍取现场面板候选、按用户剩余空槽截断；引擎账只作「策略」参照（`SatelliteLegBlock` 表头已标「策略」+「我的 N/4 槽」）。配套：`tsMap`/`rankedPx`/`etf`/`b3` 上移到 `book` 之前（`tsMap` 不再依赖 `book`，改用 `openPositions`），删掉 `offBookHeld` 特例（用户腿即全部腿）。测试 `A25TargetBlock` **15**（口径全部按用户账重写）；前端全量 **926 passed**、tsc/eslint 0 error。**同日追加**：`卫星腿` 在 timeline 行缺失时只报「最近交易日状态不可用」不给原因 → fallback 改成 `策略参照 + 加载中/加载失败（截断错误）/重试`（`timelineUnavailable/timelineError/onRetryTimeline` 三 prop，由 `PortfolioHealthCard` 接 `satQ.isLoading/satQ.error` + `invalidateQueries`）——后端同参 curl 200/243 行正常，疑是浏览器端查询失败，待用户回传错误文本。**信息提炼（用户原话「信息不够醒目」）**：`SatelliteLegBlock` 把**实盘账本（我的）移到表头正下方**（原先在最底部，引擎内容之后）；**策略参照（引擎操作提示）** 和 **paper 账本** 分别收进 `<details>`（summary 只留一行核心：到期卖出/持有只数/闲钱占比；前置/收益/回撤），现场面板（闸/候选/跳过）保留展开。 | — |
| OPT-242 | [x] 2026-09-24 · **H2-a25 canonical 口径收敛（用户拍板）**：`eval_sat_parking_frontier.py` 新增显式 `--sleeve-mode h2|legacy-canonical`，H2 使用 `harbor.load_etf_closes()`、`hyst_band=0.02` 和 `cashShare(T−1)`，新报告 `sat_h2_a25_2026-09-24` 不覆盖旧 0pt JSON；catalog/shared schema/UI/回测页/策略配方/SUMMARY/Watchlist 同步 H2-a25 身份。独立报告 OOS2/train/valid/long +238.4/+55.9/+3.2/+738.5，K1/K2/K4/K5 PASS，K3 FAIL（long MDD 相对纯 B3 -1.6pt），因此 canonical ≠ 全门 PASS；Live 仍为港湾。补 H2 传参、目录 variant、shared contract 与报告 verdict 测试；旧 0pt 文档/报告保留为 historical | [sat-h2-a25-2026-09-24](../backtests/stable/sat-h2-a25-2026-09-24.md) |
| OPT-184 | [ ] | **Live 侧日历过滤残余**：`market_sentiment.py`（panic cooldown 的 `SELECT DISTINCT trade_date FROM daily`）与 `watchlist_automation.py`（RS ret20 窗口）仍未按市场过滤，HK-only 日期可能进入 CN 冷却/排名窗口。验收：两处加 CN 过滤（或复用 OPT-183 的 shared helper），补测试 | — |
| OPT-185 | [x] 2026-09-14 · **策略族登记 + 星港/星舰 Timeline 组件**：前端 `strategy-settings` 扩为 4 档（港湾/母港/星港/星舰，**默认=星港**、v2 storage key + 旧键迁移）+ Settings 面板可选化（明确"仅影响展示、Live 恒港湾"）；后端新增 `service/starport.py`（`blend_starport_timeline`：母港 rows × 卫星 active 日 w=1/3 overlay + maxDD/activeDays/homeportPct 摘要，纯函数）+ `GET /timeline?strategy=starport|starship`（starship = 卫星 standalone 展示别名）+ warmup 增星港；前端 Timeline 四档切换 + 描述 + 默认档联动。测试：`tests/test_starport.py`（5）+ contract 2 + 前端 strategy-settings 重写/BacktestPage 默认档；前端 838 passed、typecheck/lint（0 error）/ruff 绿。冒烟（真实数据 2026-06-01~08-07）：`starport` → 星港 rows 48、summary `{fusedPct −6.08, homeportPct −5.87, satPct +6.63, activeDays 6}`；`starship` → 星舰 rows 49 | — |
| OPT-186 | [ ] | **Forward 数据收集（分腿 paper 记账）**：卫星腿 paper（14:30 信号/fills/滑点）+ B3 月再平衡 paper 记账；腿级账本（Harbor/B3/satellite）+ 事后混合重构（星港 w 任意档可算）。前置：OPT-178 卫星路径重接 + 执行审计电池（成本敏感 45/60/90bps、独立复现抽样、14:30 涨停接近度）。验收：三腿 paper 日更 + 腿级 NAV API + 与回测口径对账。**进展 2026-09-16（slice 1 落地）**：`GET /api/backtest/satellite-signals/live?date=`（live 14:30 候选面板只读，与 replay 同 helper；成交口径 14:30 print 用户已拍板；parity 锁 3 日 + 单元 5 + shared Zod；需重启后端生效）。**同日发现阻塞**：当日 14:30 面板 plumbing 缺失（bar_5min 无日内写入、em_probe 连败；daily 当日行 17:10 才到）→ 同日 14:30 实时计算暂不可用，slice 2（paper 账本）可先跑历史日 | — |
| OPT-187 | [x] 2026-09-14 · **Backtest 页「策略总览」展示层（5 策略）**：顶部新 tab（默认落地）分 5 子 tab——港湾/母港/星港/星舰/双子星；每档展示三窗+长窗（total/CAGR/MDD/Sharpe，冻结 clean 口径）+ 结构 + 状态徽章 + 优/劣 + 真值档路径。**子 tab 下方联动 Timeline**（同一选中策略；双子星显示退役占位）——`StrategyCatalogPanel` 受控选择 + `TimelineCard` 受控 strategy/onStrategyChange，双向同步；**策略总览内嵌 Timeline 隐藏「资金流全景」**（宏观流向不属于策略汇总，TimelineCard `showFundFlow=false`；对比页保留）。后端 `service/strategy_catalog.py`（纯展示数据，5 行全带 doc/tag/status/timelineStrategy）+ `GET /api/backtest/strategy-catalog`；`packages/shared/schemas/strategyCatalog.ts` Zod + 导出；前端 `useStrategyCatalogQuery` + `StrategyCatalogPanel`。测试：后端 4 + shared 3 + panel 3 + BacktestPage（默认 tab/联动）全量更新；frontend 853 passed / typecheck / lint 0 error / ruff 绿 | — |
| OPT-188 | [x] 2026-09-14 · **Watchlist 策略视图切换（选中哪个策略就显示对应组件）**：顶部 `StrategyModeBar`（4 档，与 Settings 默认策略同源 `useStrategyMode`）；渲染映射——港湾：`HarborDecisionCard` + `PickStrongAlignBanner` + 旧提醒折叠；母港/星港：港湾决策卡（核心腿仍适用）＋ `StrategyStatusCard`（结构/长窗 SR/MDD/优/劣 + wiring 状态）；星舰：仅 `StrategyStatusCard`（卫星 standalone，Live 路径已退役）。**组件 strategy-aware**：`HarborDecisionCard(mode)` 标题/口径随策略（星港 → 星港·今日决策 + 组合腿注记 B3 月频/卫星最近交易日有仓状态；时间线切到对应 strategy）、`PickStrongAlignBanner(mode)`（星港日对齐 + 未接线说明）；未接线的腿（B3/卫星）在卡内明示 OPT-178/OPT-186 待办。测试：`StrategyModeBar` 2 + `StrategyStatusCard` 3 + 决策卡/对齐横幅 mode 用例；前端 849 passed / typecheck / lint 0 error | — |
| OPT-189 | [x] 2026-09-14 · **每策略「今日决策」面（decision-aid）**：后端 `service/strategy_today.b3_state`（B3 腿当前月目标/漂移/再平衡清单，60d 逆波动率、月锚点、纯函数可注入数据）+ `GET /api/backtest/b3-state`；前端 `B3LegBlock`（母港/星港：本月基准日、目标权重 chips、买卖清单/无需调整）+ `SatelliteLegBlock`（星港/星舰：最近交易日有仓 n/4 槽、闸、当日成交、OPT-178/186 待接说明）+ `SatelliteDecisionCard`（星舰 standalone 决策面）并入 WatchlistPage；shared `schemas/b3State.ts` Zod + 导出；冒烟（真实数据）：asOf 2026-09-11、本月锚 2026-09-01、targets 债 86.5%（逆波动率本性）、无调整。测试：后端 3 + shared 3 + 前端 B3Leg/Satellite/卡用例；前端 852 passed / ruff / lint 0 error | — |
| OPT-190 | [x] 2026-09-14 · **Timeline 卫星口径修复（星舰/星港）**：`build_state_bucket_timeline` 沿用 frozen(next_open) 研究口径（模块 docstring 原意），导致 `strategy=starship\|starport` 产品曲线与冻结 clean 数字不一致（滚动一年星港 27.31% < 母港 39.57% 的异常）。修复：`recipe="frozen"\|"habit"` 参数 + `FROZEN_RECIPE`/`HABIT_RECIPE` 常量（single source；habit = amp_1430 + same_1430 买卖 + exit 14:30 + C1 3% + gate_1430）；route：`state_bucket` 保持 frozen、`starship`/`starport` 走 habit；附带 `sgap_to_timeline_rows` maxDD 符号统一为负。验证（长窗 2021-08~2026-08）：星港 **198.29**（activeDays 630）/ 星舰 **463.57** / 母港 116.34 / 港湾 201.5 vs 冻结 198.1/463.6/116.3/201.5；滚动一年：星港 **39.79** vs 母港 39.57。测试：recipe 转发 + `state_bucket_track` 47 全绿 | — |
| OPT-191 | [x] 2026-09-14 · **双子星转并行对照档 + Timeline 接线（用户决策）**：不退役、去掉 REJECT 徽章——catalog status → `parallel_candidate`/「并行对照」、`timelineStrategy="twin_star"`、cons 保留事实（valid Δ−21.2/Δsr−0.65）去掉判决措辞；`blend_overlay_timeline` 泛化（星港 base=母港 w=1/3；双子星 base=港湾 w=1/2）+ route `strategy=twin_star`（habit 卫星）+ 前端 `TimelineStrategy` 5 档、总览内嵌曲线（隐藏资金流）；共享 schema 枚举加 `parallel_candidate`/`twin_star`。验证（长窗）：双子星 **291.94** / 港湾底座 201.5 / MDD −20.8 / activeDays 630（冻结 +291.9）。**默认数据收集档仍=星港（不动）**；5 套并行对比 | — |
| OPT-192 | [x] 2026-09-14 · **星舰执行审计（H-SAT-EXEC）**：`scripts/audit_sat_execution.py`（只读：成本敏感性 30/45/60/90bps + 入场可交易性 + 容量）→ 报告 JSON + [`sat/sat-execution-audit-2026-09-14.md`](backtests/sat/sat-execution-audit-2026-09-14.md)。结果（long 窗 1040 fills，基线复现 +463.6/3.50/−8.4）：**90bps 仍 +310.3%/SR 2.45/MDD −19.8**；溢价 median +4.88%、**贴板 0**、近涨停 4/1040（0.38%）、range 0 例外；ADV median 0.76 亿、4×25% 槽位占比 ¥1M 0.33%/¥5M 1.65%/¥20M 6.58% → **容量 ≤5M 舒适**。**结论：执行层通过**（星舰前置清单第 1 项完成）；残余=采样 bar 代理（51.5% fill 的 amp 只反映采样窗）+ >5M 冲击未建模 | — |
| OPT-193 | [x] 2026-09-14 · **Watchetlist 决策面去重 + 卫星腿字段修复**：① `PortfolioHealthCard` 内遗留的第二个「港湾 · 今日决策」标题改为「核心腿状态（港湾配方）」（它是核心腿/停车状态，非独立策略决策；独立决策面 = 上方策略卡）；② `blend_overlay_timeline` 行补齐 `satPositions/satSlots/filledToday/idleSlots/gateOpen`（星港/双子星行此前缺失 → 前端显示"有仓 0/0 槽"假象）；③ `SatelliteLegBlock` 新增"今日有成交（现持 0/n 槽）"态（有平仓无持仓日不再误显示 0/0）。测试：后端 8（starport）+ 前端 SatelliteDecisionCard 两个态；855 passed | — |
| OPT-194 | [x] 2026-09-14 · **Watchlist 减负（用户 UI 反馈）**：① `PickStrongAlignBanner` 对齐时**静默**（verdict=aligned → null），未对齐时显示并移到**决策卡上方**；② 删除「策略状态卡」`StrategyStatusCard`（星港/母港/星舰视图，Backtest 总览已承载；组件+测试删除）；③ 删除「展开旧提醒（轮动 / Gate 详情）」+ `ThirdAssetSleeveBanner`（组件+测试删除，后端 T6 收敛仍见 OPT-179）；④ 删除 Execution Gate 横幅（`executionGate` 计算保留，仍供 table/journal 用）；⑤ `WatchlistToolbar` 删除描述文字（Manage/Names/Scores updated/Last automation）；⑥ PortfolioHealthCard 去重：删「核心腿状态」标题与 `PickStrongOpsPanel`（**2026-09-14 同日被 OPT-195 修正**——该面板实为核心引导内容，已恢复并升级为策略版本）。测试：对齐条 2 例重写、toolbar 2 例移除；846 passed / typecheck / lint 0 error | — |
| OPT-195 | [x] 2026-09-14 · **单一策略引导组件（用户澄清修正 OPT-194⑥）**：删掉顶部 `HarborDecisionCard`（「星港 · 今日决策」壳）与 `SatelliteDecisionCard`（组件+测试删除）；**恢复** `PickStrongOpsPanel` 操作引导（今日 pick/停车/步骤/记录买入）并提升为 Watchlist 唯一决策组件（`PortfolioHealthCard`，位于策略条/日对齐条下方），新增 `mode` 版本：**港湾**=核心引导+体检+对账+股票篮；**母港**=+B3 月再平衡；**星港**=+B3+卫星腿；**星舰**=仅卫星腿（核心内容隐藏）；`SatelliteLegBlock` 拆为独立文件。测试：840 passed / typecheck / lint 0 error | — |
| OPT-196 | [x] 2026-09-14 · **5 档策略视图 + 去「未审计」全局（用户 UI 反馈）**：① `StrategyMode` 增 `twin_star`（双子星 · 并行对照）→ Watchlist 策略条/Settings 自动进第 5 档；PortfolioHealthCard 对 twin_star 渲染港湾核心引导 + 卫星腿（timeline `strategy=twin_star`），`PickStrongAlignBanner` 注记「卫星 1/2 overlay」；② 星舰「激进 · 未审计」→「**激进 · 前置未满**」（settings tag/描述、catalog status `aggressive_unaudited`→`aggressive_pending` + shared schema 枚举同步、BacktestPage 子标题/注记、`SatelliteLegBlock`）；pros/cons 引 H-SAT-EXEC 结果（90bps +310%/SR 2.45、贴板 0、容量 ≤5M；进 Live 前置 = paper 3/20 + 用户风险授权）。测试：前端 841 passed / typecheck / lint 0 error；后端 catalog+contract 15 passed | — |
| OPT-197 | [x] 2026-09-14 · **决策卡区域标题 + 区块间距（用户 UI 反馈）**：① `PortfolioHealthCard` 主体改**单列 `gap-3`**（核心块 + B3/卫星腿同列，去掉 legs 的 `mt-2`）——修复「卫星腿/停车块黏在今日 pick 上」；② 新增区块标题：**操作引导**（港湾核心 · 100% 硬切）、**停车体检**（闲置现金 ETF 停车场）——accent-bar 样式；③ 标题字重统一：核心腿对账 / B3 月再平衡 / 卫星腿 → `font-semibold`。测试：841 passed / typecheck / lint 0 error | — |
| OPT-198 | [x] 2026-09-14 · **核心腿对账红牌修复 + paper 镜像自愈（用户反馈）**：诊断——用户的 registry 实际 100% OIL（user_trades 09-11 有 ADD），红牌来自 **paper 账本**：`sleeve_paper_auto` 从未把 ETF 停车腿入账（09-11 那次 `resolve_next_open_fill` 无 pending 占位 → 静默 no-op；09-14 服务 18:20 未运行，且该 job **不在启动 EOD catch-up 链**）→ recon 按空 paper 账本重放 →「缺买 ETF:513350」。修复：① `sleeve_paper_recon._decision_day`（next_open 行按 `signalDate` 计当日 flow/预持仓；backfill/补跑不再误报 `extraOpens`）；② `catchup_missed_eod_chain` 增 `sleeve_paper_auto`（≥18:25 guard）；③ 前端对账卡 paper 标签改「镜像已买/已卖」+「缺买/缺卖 = paper 自动镜像差异，非你的手动成交」注记；④ **数据修复**：`apply_sleeve_to_paper(day=2026-09-11)` 补入 ETF:513350（entry 2026-09-14 @1.42、sleeve 100%）→ recon 09-11 aligned、09-14 HOLD/idle 全绿。测试：recon+job+auto 33、scheduler catchup 9（含新 guard 用例）；ruff clean | — |
| OPT-199 | [x] 2026-09-14 · **星舰回测明细（用户 UI 反馈）**：Timeline 卫星口径此前只有一行 `S-GAP` + 累计 NAV，持仓/持有列空白、流水缺失。① 日表卫星行：徽章「卫星S-GAP」（violet）、持仓=`卫星 N仓 · 空X槽 · 闸关`、持有=`成交 N · 卖 {symbols}`（blotter 按 `exitDate` 归集）、卖出列=当日出场标的、基线/超额列 `—`（无基线腿）；② 新增「**卫星腿明细**」区块（仅响应带 `blotter`/`openPositions` 时）：当前持仓表（代码/入场日/入场价/现价/已持/到期/盈亏）+ 成交流水表（日期/类型/代码/amp%/amp_1430 排名/入场→出场/盈亏/贡献/原因映射）+ 跳过计数（T+1/14:30，不计入）。③ 类型：`TimelineBlotterRow`/`TimelineOpenPosition` 加进 `lib/queries/backtest.ts`。测试：BacktestPage 12 passed（新增卫星明细用例）；全量 842 passed / typecheck / lint 0 error | — |
| OPT-202 | [x] 2026-09-15 · **策略族 regime 经验固化（catalog 字段 + 面板）**：用户拍板把五家策略的"什么市况适合/不适合"写进系统。① 诊断脚本 `diag_family_regime.py`（只读）：年度/月度/市况分桶（510300 T−1 标签）→ `family_regime_2026-09-15.json`；② 共享 schema 加 `StrategyCatalogRegimeSchema`（fit/unfit/evidence/note，可选）+ 单测；③ 后端 `strategy_catalog` 五家全部替换 pros/cons（详细数据）+ 每家 `regime` 块（`strategy_catalog.py` 深拷贝 regime 防串改）；④ 前端 `StrategyCatalogPanel` 渲染「市场适配」区（适合/不适合 + 证据表）+ 面板测试；⑤ 文档 `docs/backtests/family-regime-2026-09-15.md` + SUMMARY。测试：catalog 5 passed · 面板 4 passed | — | [x] 2026-09-15 · **星舰 v2 固化（回测/展示层）**：用户拍板 `A2_true` = 卫星 + 闲置现金停 ETF 套筒（真实现金权重 causal T−1、5bps/边）。① 引擎：`replay_sgap_from_context` 行加 `cashShare`（真实现金 = (1+realized−n×clip)/nav，固定 25% clip）；② 服务：`compose_parked_rows`（纯函数，因果、含转移成本）+ `apply_parked_display`（显示字段换 parked、`satNav` 留 standalone 以免污染星港/双子星）；③ API：`strategy=starship` 传 `parked_display=True`；④ 前端：`parkedNav/parkedReturnPct/parkedWeight/cashShare` 类型、日表「停车 x%」、标题「停车均值 x%」、v2 副标题/note、`harborHoldLine` 加停车；⑤ 目录 `strategy_catalog` 星舰 v2 数字 + `UPDATED=2026-09-15`；⑥ 文档/AGENTS 定义变更留档。测试：后端 +3（composition/display）+ catalog 4、前端 845 passed / typecheck / lint；服务层实测复现 long +884.81/MDD −28.5（Δ<0.1） | — | [x] 2026-09-14 · **星舰 Timeline 持仓条（S-GAP）修复**：`buildHarborSegments` 此前把卫星行当 PARK（`pick='S-GAP'` 未知 → 灰块、块内标 `S-GAP`），持股/空闲不可分。现在：① 新增 `mode:'SAT'` 段（`ident=SAT:{pos}:{g|x}`，capacity=`SAT_CAPACITY=4` 对齐 `MAX_POS`），块色 = 实心紫（持仓/4槽，渐变分界）+ 淡紫（空闲槽）+ 斜纹（闸关）+ 灰（空仓）；② `harborHoldLine`/tooltip/段 title 改「卫星 N/4仓 · 空X槽 · 闸关 · 成交N」；③ 图例与占比 chips 改按槽位分布（卫星 N/4仓 x天 + 闸关 x天），无基线腿时隐藏「基线/基线累计」。测试：harbor-segments 9、BacktestPage 13、全量 **844 passed** / typecheck / lint 0 error | — |

### E 策略实现准确度（2026-09-11 新增）✅ 全完成（OPT-171/172/173）

> 把"前视/时钟/成本"那类事故（OPT-159 前视、14:30 时钟统一、OPT-154 滑点双计、OPT-157 qfq 混基准）从**人工审计**变成**机器护栏**。

---

### 刚完成（待归档）

| OPT-175 | [x] 2026-09-12 · **coverage 余量**：88.05% → **88.93%**（门 88 恢复安全垫）；新增 `test_fin_panel`(12)/`test_cn_fin_statements_sync`(11)/`test_commodity_signals`(17)；全量 4345 passed | — |
| OPT-176 | [x] 2026-09-12 · **散户关注度快照基建**：新表 `cn_xq_follow`（migration `0049`，雪球 `stock_hot_follow_xq` 全市场关注数）+ 每日 job `xq_follow_snapshot`（工作日 15:40 Asia/Shanghai）+ `scripts/sync_xq_follow.py`；SYNC_JOB_TYPES + `SCHEDULER_JOB_CATALOG`（factors 组）；`tests/test_xq_follow.py` 4 passed；无历史接口，只能向前每日积累（P0-13） | — |
| OPT-177 | [x] 2026-09-12 · **ETF trail8 前视 bug（双子星核心回测污染）**：`pick_strong_grid.build_nav_from_cache` 与 `pick_strong_track.build_mom_compare_timeline` 的 trail 用**当日收盘**触发、却把**当日收益记 0** → 1 日前视。实测 long 窗 `fusedPct 40.2 → 0.8`（因果）、valid `56.9 → 20.0`。**修复**：trail 改 t-1 收盘触发（`trail_causal` 默认 True；`build_mom_compare_timeline` 同步）；**连锁**：B8 修正后 T3 连收益都反超 T0、B9 A2/A3 变 PASS。审计档 [audit-trail8-2026-09-12](backtests/audit-trail8-2026-09-12.md) | — |
| OPT-171 | [x] 2026-09-12 · **PiT 前视护栏**：`build_panel` 拆出纯 `build_panel_from_px`；`tests/test_pit_no_lookahead.py` 钉死"上月流动性选集"契约 + 禁 `.shift(-` | — |
| OPT-172 | [x] 2026-09-12 · **配方 attestation 扩展**：`state_bucket_track.COSTS_ROUNDTRIP` 改为从 `paper_cost_model` 派生（单一源）；attest CN 30bps / HK 90bps、回测成本==live、engine 用共享模型 | — |
| OPT-173 | [x] 2026-09-12 · **回放黄金**：`tests/test_engine_golden.py` + `tests/golden/engine_summary.json` 冻结 `_summarize` 指标（`UPDATE_GOLDEN=1` 再生） | — |
| OPT-165 | [x] 2026-09-12 · **schema parity 护栏**：`tests/test_schema_parity.py`（纯 migration-coverage + PG 列超集）；**抓到真 drift**——`system_events` 无 migration → 补 `0048_system_events`（head 0048）；全量 4298 passed | — |
| OPT-166 | [x] 2026-09-12 · **死代码/omit 收敛**：`state_bucket_slice.py` 移出 `src/`→`scripts/`（无 src 引用，文档已更新），删 coverage omit；engine 6 候选行复核为防御 nil 守卫、保留 | — |
| OPT-174 | [x] 2026-09-12 · **main 变绿 hotfix**：修 ruff 7 + pyright 7（`ruff check`/`pyright` 均 0 error；全量 4295 passed） | — |
| OPT-164 | [x] 2026-09-12 · **CI 格式门**：一次性 `ruff format`（432 文件）+ `prettier --write`（251 源码）；`lint` 加 `ruff format --check`、root `format:check` + CI 步骤 | — |
| OPT-162 | [x] 2026-09-12 · 工作区 WIP 按主题切分提交（5 个 commit：OPT-160/161 工程 · sat 时钟统一 · trend_guide 钩子 · 数据一致性 · P0-12 研究+因子库；`git status` 干净） | — |

| OPT-160 | [x] 2026-09-12 · 运行产物去跟踪分类治理（111 文件 `git rm --cached`；`.gitignore` +4 规则 +`coverage_tmp.json`；README 再生来源表；报告测试 57 passed） | [2026-09-12-opt-160-artifact-untrack.md](archive/2026-09-12-opt-160-artifact-untrack.md) |
| OPT-161 | [x] 2026-09-12 · 文档死链清零（根因：`opt-151/152` 归档被误放 `docs/optimization-checklist/archive/` → `git mv` 到 `docs/archive/`；`linkcheck` clean 173 文件） | — |

| OPT-156 | [x] 2026-09-11 · stock_dailybasic 2023-12-29→2024-08-01 空洞回填（**实缺 2024-01~07**，140 个 A 股交易日/74.8 万行）+ trade_calendar SSE 补齐到 2005（+6575 行，4374 open days） | — |
| OPT-157 | [x] 2026-09-11 · CN `daily` 复权统一重建 + A 股日线回填 2007 + 财报回填 2007–2017 + 两融空洞补全（原 close 是标准 qfq/陈旧基准/未复权混合，跨除权收益跳变；重建后 2007–2026 单一基准，全市场一致性 99.8%→100%，脚本 `backfill_cn_daily_history`/`rebuild_cn_daily_qfq`/`fix_daily_outliers`/`backfill_cn_financials_history`，备份 `daily_backup_20260911`） | [data-consistency-2026-09-11.md](backtests/data-consistency-2026-09-11.md) |
| OPT-158 | [x] 2026-09-11 · 指数历史回补：`index_daily` 000300/000905/000001 等 5 指数经 `index_hist_extend.py --since 2005-01-01` 补 2005–2020（+20870 行），支撑真实市值加权 beta（DH-2） | [a1-voltarget-beta-2026-09-11.md](backtests/a1-voltarget-beta-2026-09-11.md) |
| OPT-159 | [x] 2026-09-11 · P0-12 pilot 前视审计：`strategy_a1_voltarget.build_panel` 用本月末成交额选本月票（前视 ~+20pt/yr）→ 改上月末 as-of；沉淀纪律"选池必须调仓日可得"；同批 `diag_tenbagger` 审计为 as-of 正确 | [a1-voltarget-beta-2026-09-11.md](backtests/a1-voltarget-beta-2026-09-11.md) |

| OPT-155 | [x] 2026-09-10 · ADV 冲击诊断（AUM ¥1000万 → 不 material，关闭；复活条件 ¥3000万+） | [2026-09-10-opt-155-adv-impact.md](archive/2026-09-10-opt-155-adv-impact.md) |

| OPT-154 | [x] 2026-09-10 · E5 滑点双计修正（回测成本 = live 成本模型，CN 30bps / HK 90bps） | [2026-09-10-opt-154-e5-slippage.md](archive/2026-09-10-opt-154-e5-slippage.md) |

| OPT-153 | [x] 2026-09-10 · 卫星腿判定修正（自选卫星单不再误报偏离 + 腿推断去 `===12.5`） | [2026-09-10-opt-153-sat-leg-audit.md](archive/2026-09-10-opt-153-sat-leg-audit.md) |

| OPT-152 | [x] 2026-09-09 · Timeline 实盘口径主曲线（A 方案：S-3 实际权益主曲线 + 100% 押注基准降虚线） | [2026-09-09-opt-152-timeline-product-curve.md](archive/2026-09-09-opt-152-timeline-product-curve.md) |

| OPT-151 | [x] 2026-09-09 · 核心腿 recon 对账闭环（sleeve_paper_recon + brief/job/API/UI） | [2026-09-09-opt-151-sleeve-core-recon.md](archive/2026-09-09-opt-151-sleeve-core-recon.md) |

| OPT-150 | [x] 2026-09-09 · 账本修正机制 PATCH 改腿 + 审计一键挪链 | [2026-09-09-opt-150-trade-correct.md](archive/2026-09-09-opt-150-trade-correct.md) |

| OPT-149 | [x] 2026-09-09 · user_trades 腿拆分 sat/s3 + 审计按腿分支（卫星对 paper 账） | [2026-09-09-opt-149-trade-legs.md](archive/2026-09-09-opt-149-trade-legs.md) |

| OPT-147 | [x] 2026-09-09 · R-wide breadth 池 HK 污染量级断言（flips 0/0/0 关项，引擎零改动） | [2026-09-09-opt-147-rwide-hk.md](archive/2026-09-09-opt-147-rwide-hk.md) |

| OPT-148 | [x] 2026-09-08 · paper HK T+2 settled 账本 + 平安最高佣金 90bps + swap 费用归位 | [2026-09-08-opt-148-hk-settle-ledger.md](archive/2026-09-08-opt-148-hk-settle-ledger.md) |

| OPT-124 | [x] 2026-09-06 · 多 token 轮换 + 配额看门狗 | [2026-09-06-opt-124-tushare-pool.md](archive/2026-09-06-opt-124-tushare-pool.md) |

---

---

| OPT-125 | [x] 2026-09-06 · DB 连接池 + 重试 + 慢查询护栏 | [2026-09-06-opt-125-db-pool.md](archive/2026-09-06-opt-125-db-pool.md) |
| OPT-126 | [x] 2026-09-06 · 东财出口探针 + 熔断可视化 | [2026-09-06-opt-126-em-probe.md](archive/2026-09-06-opt-126-em-probe.md) |
| OPT-145 | [x] 2026-09-06 · 外购分钟对拍 A 形式化 | [2026-09-06-opt-145-vendor-minute.md](archive/2026-09-06-opt-145-vendor-minute.md) |
| OPT-146 | [x] 2026-09-06 · 港股符号误标 + 结算语义 | [2026-09-06-opt-146-hk-symbol.md](archive/2026-09-06-opt-146-hk-symbol.md) |

---

---

## 冬眠（3 条 · 未关闭 · 可唤醒）

> 陈年 open / P2 储备，移出未完成队列减负。唤醒 = 移回"未完成"并排优先级。

| OPT | 状态 | 全文 |
|-----|------|------|
| OPT-045 | [ ] Phase A done · B/C 待开 | [2026-09-06-opt-dormant-open.md](archive/2026-09-06-opt-dormant-open.md) |
| OPT-075 | [ ] 2026-08-12 登记未处理 | 同上 |
| OPT-127 | [ ] P2 储备（用户判断暂无用；OPT-125 后重估） | 同上 |

---

## 已完成索引（108 条 · 按天归档 · 只读）

| 日期 | OPT | 标题 | 归档 |
|------|-----|------|------|
| 2026-06-18 | 031–040 | Sentiment 降载 / ensure_table / 指数批量 / 信号去重 / 退役串行 / Screener / Dashboard / RSS / Query 迁移 | [2026-06-18-opt-031-040-perf-queries.md](archive/2026-06-18-opt-031-040-perf-queries.md) |
| 2026-07-29 | 041–044 | HK 闸门 / ETF 通用化 / HK 日线 cron / watchlist 显示 | [2026-07-29-opt-041-044-hk-etf.md](archive/2026-07-29-opt-041-044-hk-etf.md) |
| 2026-08-01 | 045 Phase A | /v1 API 整圈（045+046+047 Phase A；正文 045 留 B/C） | [2026-08-01-opt-045-v1-api-surface.md](archive/2026-08-01-opt-045-v1-api-surface.md) |
| 2026-08-01 | 046–048 | /v1 业务 endpoint / explain 文档 / Tunnel 部署 | [2026-08-01-opt-046-048-api-v1.md](archive/2026-08-01-opt-046-048-api-v1.md) |
| 2026-08-01 | 049 | Paper-trading 启动 | [2026-08-01-opt-049-paper-trading.md](archive/2026-08-01-opt-049-paper-trading.md) |
| 2026-08-01 | 050 | 数据源质量审计 | [2026-08-01-opt-050-data-source-audit.md](archive/2026-08-01-opt-050-data-source-audit.md) |
| 2026-08-01 | 051 | API key 配额 openapi | [2026-08-01-opt-051-api-key-quota-openapi.md](archive/2026-08-01-opt-051-api-key-quota-openapi.md) |
| 2026-08-01 | 052 | Alpha Radar HK | [2026-08-01-opt-052-alpha-radar-hk.md](archive/2026-08-01-opt-052-alpha-radar-hk.md) |
| 2026-08-01 | 053 | DB 走向决策 | [2026-08-01-opt-053-db-direction.md](archive/2026-08-01-opt-053-db-direction.md) |
| 2026-08-01 | 055 | AI agent cookbook | [2026-08-01-opt-055-ai-agent-cookbook.md](archive/2026-08-01-opt-055-ai-agent-cookbook.md) |
| 2026-08-01 | 056 | Docker 一键起 | [2026-08-01-opt-056-docker-one-click.md](archive/2026-08-01-opt-056-docker-one-click.md) |
| 2026-08-01 | 057 | TV Capture 三轨（另一条 057 见 08-31 档） | [2026-08-01-opt-057-tv-capture-three-track.md](archive/2026-08-01-opt-057-tv-capture-three-track.md) |
| 2026-08-02 | 058 | 漏斗 N 日表格（另一条 058 见 08-31 档） | [2026-08-02-opt-058-funnel-history-paper-v0.1.md](archive/2026-08-02-opt-058-funnel-history-paper-v0.1.md) |
| 2026-08-03 | 059 | legacy 清理（与 09-01 档的 059 涨停审计不同条） | [2026-08-03-opt-059-legacy-cleanup.md](archive/2026-08-03-opt-059-legacy-cleanup.md) |
| 2026-08-04 | 060 | Tauri 降级（另一条 060 见 09-01 档） | [2026-08-04-opt-060-tauri-deprecation.md](archive/2026-08-04-opt-060-tauri-deprecation.md) |
| 2026-08-04 | 061 | DB 备份迁移 | [2026-08-04-opt-061-db-backup-migrate.md](archive/2026-08-04-opt-061-db-backup-migrate.md) |
| 2026-08-07 | 062 | Paper v0.2 | [2026-08-07-opt-062-paper-v02.md](archive/2026-08-07-opt-062-paper-v02.md) |
| 2026-08-07 | 063 | 回测引擎 v0 | [2026-08-07-opt-063-backtest-engine.md](archive/2026-08-07-opt-063-backtest-engine.md) |
| 2026-08-07 | 064 | 卖出归因 + 回测页 | [2026-08-07-opt-064-exit-attribution-backtest-page.md](archive/2026-08-07-opt-064-exit-attribution-backtest-page.md) |
| 2026-08-07 | 065 | 周度复盘 | [2026-08-07-opt-065-weekly-review.md](archive/2026-08-07-opt-065-weekly-review.md) |
| 2026-08-07 | 066 | journal 防御 | [2026-08-07-opt-066-journal-symbol-defense.md](archive/2026-08-07-opt-066-journal-symbol-defense.md) |
| 2026-08-07 | 067 | 相关性防火墙 | [2026-08-07-opt-067-correlation-firewall.md](archive/2026-08-07-opt-067-correlation-firewall.md) |
| 2026-08-09 | 068–073 | 真实交易看板 / K 线源 / 入池闸门 / score 回填 / RS 过滤 / 持仓体检 | [2026-08-09-opt-068-073-engine-batch.md](archive/2026-08-09-opt-068-073-engine-batch.md) |
| 2026-08-12 | 074–087（无 075、077；075 未完成在正文，077 从未立项） | 健壮性审查 / 执行闸统一 / 体检提醒 / 对账 / BacktestPage / 通知 / 周报 / 条件单 / 信息层 / C4 框架 | [2026-08-12-opt-074-087-robust-batch.md](archive/2026-08-12-opt-074-087-robust-batch.md) |
| 2026-08-12 | 085–089 | S-3 信息层 + C4 对照框架 + 体检卡（当时总结） | [2026-08-12-opt-085-089-s3-info-layers.md](archive/2026-08-12-opt-085-089-s3-info-layers.md) |
| 2026-08-12 | 090–094 | webhook + 一键启动 + 红绿灯 | [2026-08-12-opt-090-094-webhook-lights.md](archive/2026-08-12-opt-090-094-webhook-lights.md) |
| 2026-08-13 | 095–110（无 101，从未立项） | 退出对齐 / HK 信号 / 蒙特卡洛 / 涨跌停 / ATR / 对账 / LLM 移峰 / Alpha 初验 | [2026-08-13-opt-095-110-align-batch.md](archive/2026-08-13-opt-095-110-align-batch.md) |
| 2026-08-14 | 111–118 | 对账横幅 / cron / 执行卡 / Webhook 目录 / Bark / Tunnel / 手机 UI / 网关 | [2026-08-14-opt-111-118-mobile-webhook.md](archive/2026-08-14-opt-111-118-mobile-webhook.md) |
| 2026-08-21 | 119–121 | 套筒模拟器 / 核心仓核对 / dual 联合验收 | [2026-08-21-opt-119-121-sleeve-dual.md](archive/2026-08-21-opt-119-121-sleeve-dual.md) |
| 2026-08-31 | 057–058 | dailybasic 停更修复 / 双子星 14:30 决策审计 | [2026-08-31-opt-057-058-dailybasic-twin-audit.md](archive/2026-08-31-opt-057-058-dailybasic-twin-audit.md) |
| 2026-09-01 | 059–060 | 涨停审计 / 双子星 v2 | [2026-09-01-opt-059-060-limitup-twin-v2.md](archive/2026-09-01-opt-059-060-limitup-twin-v2.md) |
| 2026-09-01 | 122–123 | 盘中近似信号 / v3 固化 | [2026-09-01-opt-122-123-twin-signal.md](archive/2026-09-01-opt-122-123-twin-signal.md) |
| 2026-09-02 | 128–135 | clip4 / 对齐 / Timeline / 占用 / blotter / 健康 / Zod / 日流程 | [2026-09-02-opt-128-135-twin-live.md](archive/2026-09-02-opt-128-135-twin-live.md) |
| 2026-09-03 | 136–137 | body=3 对齐 / 5 分钟线 | [2026-09-03-opt-136-137-sat-live.md](archive/2026-09-03-opt-136-137-sat-live.md) |
| 2026-09-04 | 138 | 红套件清零 | [2026-09-04-opt-138-red-suite-green.md](archive/2026-09-04-opt-138-red-suite-green.md) |
| 2026-09-04 | 139 | Scheduler 上报统一 | [2026-09-04-opt-139-job-guard.md](archive/2026-09-04-opt-139-job-guard.md) |
| 2026-09-04 | 140 | 模式口径统一 | [2026-09-04-opt-140-leg-split.md](archive/2026-09-04-opt-140-leg-split.md) |
| 2026-09-04 | 141 | 习惯口径引擎化 | [2026-09-04-opt-141-live-caliber.md](archive/2026-09-04-opt-141-live-caliber.md) |
| 2026-09-04 | 142 | 日历收敛 + Alembic | [2026-09-04-opt-142-calendar-api-migration.md](archive/2026-09-04-opt-142-calendar-api-migration.md) |
| 2026-09-04 | 143–144 | 可重放补强 / 外围抖动 | [2026-09-04-opt-143-144-replay-quiet.md](archive/2026-09-04-opt-143-144-replay-quiet.md) |
| 2026-09-06 | 124 | 多 token 轮换 + 配额看门狗 | [2026-09-06-opt-124-tushare-pool.md](archive/2026-09-06-opt-124-tushare-pool.md) |
| 2026-09-06 | 125 | DB 连接池 + 重试 + 慢查询护栏 | [2026-09-06-opt-125-db-pool.md](archive/2026-09-06-opt-125-db-pool.md) |
| 2026-09-06 | 126 | 东财出口探针 + 熔断可视化 | [2026-09-06-opt-126-em-probe.md](archive/2026-09-06-opt-126-em-probe.md) |
| 2026-09-06 | 145 | 外购分钟对拍 A 形式化 | [2026-09-06-opt-145-vendor-minute.md](archive/2026-09-06-opt-145-vendor-minute.md) |
| 2026-09-06 | 146 | 港股符号误标 + 结算语义 | [2026-09-06-opt-146-hk-symbol.md](archive/2026-09-06-opt-146-hk-symbol.md) |
| 2026-09-09 | 147 | R-wide breadth 池 HK 污染量级断言（关项） | [2026-09-09-opt-147-rwide-hk.md](archive/2026-09-09-opt-147-rwide-hk.md) |
| 2026-09-09 | 149 | user_trades 腿拆分 sat/s3 + 审计按腿分支 | [2026-09-09-opt-149-trade-legs.md](archive/2026-09-09-opt-149-trade-legs.md) |
| 2026-09-09 | 150 | 账本修正机制 PATCH 改腿 + 审计一键挪链 | [2026-09-09-opt-150-trade-correct.md](archive/2026-09-09-opt-150-trade-correct.md) |
| 2026-09-09 | 151 | 核心腿 recon 对账闭环（sleeve_paper_recon + brief/job/API/UI） | [2026-09-09-opt-151-sleeve-core-recon.md](archive/2026-09-09-opt-151-sleeve-core-recon.md) |
| 2026-09-09 | 152 | Timeline 实盘口径主曲线（A 方案：S-3 实际权益主曲线 + 基准降虚线） | [2026-09-09-opt-152-timeline-product-curve.md](archive/2026-09-09-opt-152-timeline-product-curve.md) |

> **历史重号说明**：OPT-057/058/060 在正文出现过两次（不同日期不同内容），OPT-059 的归档文件与正文条目主题不同。
> 一律不重编号，以"日期+标题"区分：057 = 08-01 TV Capture / 08-31 dailybasic 停更；058 = 08-02 漏斗 / 08-31 双子星 14:30 审计；
> 060 = 08-04 Tauri / 09-01 双子星 v2；059 归档 = 08-03 legacy 清理，正文 059（09-01 涨停审计）在 09-01 档。
