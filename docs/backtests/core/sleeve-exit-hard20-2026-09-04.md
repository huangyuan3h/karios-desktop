# 套筒尾险：20d −10% 硬切 GC001 三窗（2026-09-04）

> **一句话**：单 ETF 套筒上加 20 日高点 −10% 硬切回 GC001，三窗 vs 无硬切 base：OOS2/train 数字完全一致（零触发、无信息），valid +30.1 vs +23.1（+7.0/DD−3.2 单窗亮）。两窗零触发 + 单窗亮 = 证据不足，**不进实盘套筒，记观察**。
> **关键词**：套筒 尾险 硬切 GC001

**脚本**：`services/data-sync-service/scripts/sleeve_exit_variants.py`（`exit_mode=hard20` vs `base`/`trail8`，`min_idle_pct=0.0`）
**规则**：三窗铁律；单窗好看 = 过拟合，不进 Live。

---

## 1. 结果（套筒总收益 / 相对基线增量 / DD）

| 模式 | OOS2 | train | valid |
|------|------|-------|-------|
| base（无硬切） | 87.3 / +3.2 / 6.4 | 64.2 / +15.1 / 6.2 | 61.9 / +23.1 / 13.7 |
| **hard20（20d −10% 切 GC001）** | 87.3 / +3.2 / 6.4 | 64.2 / +15.1 / 6.2 | **68.9 / +30.1 / 10.5** |
| trail8（对照） | 87.3 / +3.2 / 6.4 | 64.9 / +15.7 / 6.2 | 69.8 / +31.0 / 10.0 |

## 2. 判定

- **OOS2/train 零触发**：hard20 与 base 小数点后都一致，说明 −10% 线在这两窗一次没碰到——该变体在这两窗没有提供任何信息，不是“验证通过”。
- **valid 单窗亮但不够**：+7.0/DD−3.2 方向对（尾险切掉了 valid 的一段回撤），但按铁律单窗亮不能进 Live；且 valid 套筒 DD 10.5 仍远高于基线 2.7，尾险问题本身还在。
- **不改套筒规则**（现行：mom60+MA200 轮动，Live 侧 trail8 止损提示）；hard20 记观察，若未来窗口出现触发再重验。

复现：

```bash
cd services/data-sync-service
PYTHONPATH=src python3 scripts/sleeve_exit_variants.py
```
