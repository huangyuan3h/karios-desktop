# 习惯口径冻结成绩单：C1 + 14:30 买/卖三窗重放（2026-09-04）

> **一句话**：Live 跑的同一套配方（当天缺口→14:30 买→C1 3%→body=3→第 3 日 14:30 卖）在今日代码+数据上重放三窗，相对核心 **OOS2 +76.3/+2.22/−1.9 · train +14.5/+1.53/−2.7 · valid +2.7/+0.21/0 → beats_core**，与 09-03 逐数一致。新增成交来源血统：入场 100% 真 14:30 bar，出场 ~5% 回退到收盘（已量化）。
> **关键词**：习惯口径 fillSrc beats_core OPT-141

**脚本**：`services/data-sync-service/scripts/compare_sat_exit_hhmm.py --save-report --report-name sat_live_caliber_2026-09-04.json`
**原始表**：`data/backtest_reports/sat_live_caliber_2026-09-04.json`
**口径**：window-local 空簿、clip4、strict `skip_t1`、当天缺口 + 14:30 买、C1 3%、body=3、第 3 日 14:30 卖、核心择强 trail8、`opp_50`。冻结 T 开盘引擎不动。

## 1. 为什么重跑（OPT-141）

Live 习惯与冻结引擎是两套口径（P0-4 定论）。09-03 的 C1·14:30 行已经是三窗全过核心，
本次用今日代码重放验证成绩单不腐 + 补上缺失的一块：**回测成交价到底有多少是真 14:30 bar**。
引擎新增 `entryPxSrc/exitPxSrc`（与 paper 的 `entryPxSrc/exitPxSrc` 计数器同口径），
回放报告每窗带 `fillSrc`。

## 2. 三窗 vs 核心（c1_x1430 = Live 习惯）

| 窗口 | twin tot/sr/dd | Δ核心 tot/sr/dd | 卫星腿 | fills |
|------|---------------|----------------|--------|-------|
| OOS2 | +94.1/2.94/16.1 | **+76.3/+2.22/−1.9** | +177.8/5.63/4.6 | 288 |
| train | +55.2/4.53/5.7 | **+14.5/+1.53/−2.7** | +47.9/4.43/3.6 | 132 |
| valid | +141.8/3.58/11.9 | **+2.7/+0.21/0** | +10.8/1.52/8.2 | 60 |

判定：**PASS+ / beats_core**（与 09-03 逐数一致，成绩单未腐）。

## 3. 成交来源血统 fillSrc（本次新增）

| 窗口 | entry bar_1430 | exit bar_1430 | exit 收盘回退 | 回退占比 |
|------|---------------|--------------|--------------|---------|
| OOS2 | 288 | 269 | 19 | 6.6% |
| train | 132 | 127 | 5 | 3.8% |
| valid | 60 | 56 | 4 | 6.7% |

- 入场：缺 14:30 bar 直接不买（保守），480 笔成交 100% 真 bar，无沉默乐观。
- 出场：缺 14:30 bar 回退收盘（blotter 记 `exitPxSrc=close`，以前沉默）。
  回退方向不定（232 笔真 bar 证明 14:30 卖优于收盘卖，28 笔回退里混入收盘价，
  偏向**低估** 14:30 边的实现度，而非高估）。量级 ~5%，不推翻判定。
- paper 侧：`paper_twin_star` 的 `entryPxSrc/exitPxSrc` 计数器已就绪，
  但 twin_star paper 簿当前 0 行（OPT-138 修好 CN intake 日历后才开始攒；
  等 C4 20 笔后做第一次回测-paper 血统对账）。

## 5. 14:30 bar 覆盖（OPT-143 附录 · 2026-09-04 实测）

`bar_5min` 14:30 根：3319604 行 / 5254 名 / 2024-01-02~2026-09-03，
三窗内约 5000 名/天（2026-09-03 当日 3777 名，采集窗口中低于平时 ~5200）。
结论：习惯三窗的 14:30 价**不是稀疏代理**，是全市场覆盖；fillSrc 的
100% 入场真 bar 与此一致。出场回退的 ~5% 来自个票单日缺根（停牌/极端），
不是系统性缺数据。入场缺根计数 `skipNoPrint1430` 三窗全 0——没有因为缺
14:30 bar 被沉默跳过的候选。

## 4. 判定

- 习惯口径三窗成绩单冻结为本表（Live 跟的是它，不是冻结 T 开盘）。
- 冻结引擎（next_open/收盘卖）仍是所有**新参数**的对照基线，不动。
- 残差不变：排名用全天振幅（入场名单略乐观）；出场 ~5% 收盘回退（上表）；
  valid 缓冲薄（+2.7），方差是真风险（S1 已论）。

复现：

```bash
cd services/data-sync-service
PYTHONPATH=src:scripts python3 scripts/compare_sat_exit_hhmm.py --save-report --report-name sat_live_caliber_2026-09-04.json
```
