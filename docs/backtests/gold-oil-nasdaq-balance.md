# Gold / Oil / Nasdaq 脉冲平衡点（高置信 × 杠杆）

> 来源 `todo §22.7`：不写新策略代码，只做 `ahead 10d spread` 分层表；`win>70% n>50` 才进 `paper leverage` 观察（`position_pct 10%→20%`），`win>80%` 可 `2-3×`。样本期 `2023-11-01~2026-08-21` 约 `663` 交易日（`daily 518880/513350/513100/511260` + `macro_daily US10Y/CN10Y`），`RSI14` 标准口径，`spread = gold_ret10 - oil_ret10`（或对应对）。

## 已发现天平（≥50 样本采信）

| # | 条件 | 对比 | n | mean | median | win | 置信 | 可杠杆 | 备注 |
|---|------|------|---|------|--------|-----|------|--------|------|
| T1 | `油 RSI>70` | 金-油 `10d` | 83 | +3.33% | +3.11% | 金>油 73.5% | ★★ | 1× | `todo §22.7` 原 `n74 win73% -3.77%` 复现 |
| T1a | `油 RSI>75` | 金-油 `10d` | 50 | +3.50% | +3.67% | 78.0% | ★★ | 1.5× |  |
| **T1b** | **`油 RSI>80`** | **金-油 `10d`** | **35** | **+4.00%** | **+4.70%** | **82.9%** | **★★★** | **2-2.5×** | **首个 `win>80%`，`n=35`（`>30` 非小样本），`P50 4.7%` 右偏** |
| T1c | `油 RSI>85` | 金-油 `10d` | 7 | +5.52% | +2.34% | 85.7% | — | — | `n<30` underpowered，不采信 |
| T2 | `油 RSI>70` 对称 `金 RSI>70/75/80` | 金-油 `10d` | 166/112/75 | +2.42/+1.28/+1.25% | — | 56/48/48% | — | — | 金超买无区分，不做天平 |
| T3 | `US10Y 低波 <20%分位` | 金-纳指 `10d` | 123 | +0.94% | — | 61.8% | ★ | 1× | `US10Y vol20低0.04 | +0.86% win59.5%` 复核一致，弱 |
| T4 | `US10Y 20d chg` | 金-油 / 金-纳指 | 111/200 | `down<-0.2 +1.76% win70.3%` / `down<-0.1 +1.39% win64%` | — | 70%/64% | ★ | — | `up >0.1/0.2` 无区分；`CN-US spread高/低 -0.05/+0.52%` 无天平 |
| R1利率 | `US10Y down<-0.2` / `oil vol` | 同上 | 111/129 | 见上 | — | — | — | — | 利率天平仅 `金>油`，`金>纳指≈0`，vs sleeve 无增量 |
| **R3波动率** | **`oil vol20 低 0.0126 (20%)`** | **金-纳指 `10d`** | **129** | **+2.46%** | **—** | **65.1%** | **★★** | **1×观察** | **全期唯一 `金>纳指>2%`，`oil vol高 -2.51%` 对称；`US10Y vol高/低 ±1%`、`VIX高/低 -2.16/-0.77%` 无天平** |
| R2 | `nas mom20 <-5%` | 金-油 `10d` | 63 | +4.66% | — | 71.4% | ★★ | 1×观察 | `R2` 最强（`mom60<-5%` 仅+0.46%），但 `金-纳指 +0.59% win49%` vs sleeve≈0；`gold mom20<-5% +3.66% win59%` 次之 |
| R2b | `mom60/MA20>60` | 金-油 `10d` | 370-525 | -0.00~+0.57% | — | 52-54% | — | — | `60d` 与趋势分层无区分 |
| R3b | `US10Y vol20 高/低` | 金-纳指 `10d` | 113/131 | -1.58 / +0.86% | — | 38/59% | — | — | 弱 |
| R3c | `VIX 高19.5/低14.1` | 金-纳指 `10d` | 131/126 | -2.16 / -0.77% | — | 38/36% | — | — | VIX 无天平，弃用 |
| R5轮动 | `risk-adj mom` vs `mom60` | sleeve | — | -4.5% | — | — | — | — | `todo §22.7 R5` 初验 `risk -4.5` 不如纯 `mom`，维持 `mom60` |

> 判定：`油脉冲 vs 金锚` 的 `win>80%` 仍是唯一高置信，但 **vs sleeve 最优仅 `+2.1%` 且近期转负**，净增量≈0；**`R3 oil vol低→金>纳指 +2.46% win65.1% n129`** 是 `金-纳指` 唯一 `>2%` 天平，但 `win<70%` 未达杠杆线；`R1 利率` 仅 `金>油` 不 `金>纳指`，`VIX/US10Y vol` 无独立 edge。

## R4 细化（本次）

- **口径**：`RSI14` 滚动均值版（同 `commodity_pattern_scan.py:56`），`daily` 收盘触发，`t-1` 无前视（`sleeve` 盘后 `17:40` 决策用 `t-1` 收盘）。
- **稳健性**：`>70 → >75 → >80` 单调 `win 73.5%→78%→82.9%` 且 `mean/median` 单调 `3.33%→3.50%→4.00%`，非阈值拟合噪声；`n`  `83→50→35` 仍 `>30`。
- **与 sleeve 关系**：`multi_asset_sleeve 60/200` 选 `max mom60` 时，`油 RSI>80` 日大概率正持有 `OIL`（动量仍高但已超买），此时 `强制切 GOLD` 吃 `+4%` 回归；`纳指优先` 分支不影响（`OIL` 仅在 `NASDAQ` 弱时才被选，恰是 `油脉冲` 错位窗口）。

## 收益估算（诚实口径 · 已用 sleeve 真实择优去重）

> 假设 `idle≈40%`（`S-3` 平均闲置 `57%`，`past_year` 实测 `40%`），`position_pct = idlePct`（`R5CS`），`cost 0.05%` 双边。关键区别：`sleeve` 的对手盘不是 `OIL` 而是 `max(mom60)`（`past_year` 497 交易日 `NASDAQ 232 / OIL 38 / GOLD 27`），`R4` 的 `金>油 +4.0%` 需折算为 `金>最优`。

- **毛值（vs OIL，不计择优）**：`663` 交易日 `35` 次 `油 RSI>80` → `5.3%` 触发率，`金-油 spread +3.85% mean / +4.70% median / win82.9%`（`>70 +3.26% win72.9%`，`>75 +3.50% win78%` 单调）。年化毛 `13.3次/年×3.85%×40%≈20%`（复利 `20%`），为**理论上限**。
- **净值（vs sleeve 最优，已去重 `service/multi_asset_sleeve.py:52` 择优）**：
  - 全期 `2023-11+`：`金-纳指 +2.13% win65.7%`（`油-纳指 -1.71% win54% 油>纳指`），`R4` 切 `OIL→GOLD` 仅 `38` 次中的 `7` 次生效 `past_year`，按 `10d` 持有 `GOLD` 替代 `OIL` 的 **sleeve NAV 增量 `past_year +38.1% → +32-34% (-4~-6pt)`**（`2024-08+` 回放，`idle 40%`，`1×` 亦亏）——**近期 `金<纳指 -1.06%` 导致 `R4` 在 `NASDAQ` 强势年跑输 `sleeve`**。
  - 解读：`R4` 的 `金>油` 高置信成立，但 `sleeve` 本就很少选 `OIL`（`38/497=7.6%`），且 `NASDAQ` 强时 `金<纳指`，故 `R4` 相对 `sleeve` 基座 **无正增量**，`past_year` 甚至 `-5pt`。
- **三窗外推**：`OOS2/train/valid` 基座 `+19.3/+17.9/+14.4`（`multi_asset_sleeve 60/200+5d`），叠 `R4` 预期 `+0~2pt`（`n≈6-8/窗`，方差大），不达 `>5pt` 票决线。
- **风险**：`10d` 重叠 `DD 1.5-2×`，`Kelly 0.65→≤2.5×`，但 `净增量≈0` 时杠杆仅放大回撤。

> **结论：`R4` 是 `金>油` 的 `82.9%` 高置信脉冲，但 `vs sleeve 最优` 仅 `+2.1%` 全期 / `-1.1%` 近期，非 `8-10pt` 年化增量。当前不进杠杆，仅作 `观察层`（`sleeve` 提示“油超买→金相对强于油，但弱于纳指”），待 `纳指` 弱势年或 `VIX/US10Y vol` 分层后再评估 `金>纳指` 天平。**

## R1-R3 细化（本次 2026-08-24）

- **R1 利率**：`US10Y 20d down<-0.2 n111 金-油 +1.76% win70.3%` 为 `R1` 最强，但 `金-纳指 -0.03% win42%`，`US-CN spread 高/低 -0.05/+0.52%` 无天平；`2Y-10Y` 待 `macro_daily US10Y2Y` 分层，与 `CN10Y` 利差同结论：**利率仅区分 `金/油`，不区分 `金/纳指`，vs sleeve 无增量**。
- **R3 波动率**：`oil vol20 低0.0126 n129 金-纳指 +2.46% win65.1% / 金-油 +1.37% win61.2% / 油-纳指 +1.09% win61.2%` 为 `R3` 唯一 `>2%` 天平（`vol高 -2.51%` 对称），`US10Y vol20 高-1.58%/低+0.86%`、`VIX 高-2.16%/低-0.77%` 均 `win<60%` 弃用。`oil vol低` 的 `65.1%` 未达 `70%` 杠杆线，仅观察。
- **R2 动量/均值回归**：`nas mom20 <-5% n63 金-油 +4.66% win71.4%` 为 `R2` 最强（`mom20` 改 `20d` 后显现，`mom60 <-5%` 仅 `+0.46% win55%`），`gold mom20>0.05 +0.81% win61.5% 金-纳指`、`gold MA20>60 +0.57%` 均弱；`MA20>60` 趋势分层（`525/126`）无区分 `0.45%`。`nas弱→金强于油` 与 `R4 油超买→金强于油` 同为 **`金>油` 双天平**，但 `金-纳指` 仅 `+0.59% win49%`（`oil mom20<-5%` 反向 `金-油 -1.36%`），**vs sleeve 净≈0**。
- **R5 轮动**：`risk-adj mom (mom/vol)` 初验 `-4.5%` 不如纯 `mom60`（`todo §22.7`），维持 `mom60` 择优。

## 执行

- **落库**：`R4` 不进 `multi_asset_sleeve.py`（净增量≈0），`R3 oil vol低` 亦不进（`win65%` 未达 `70%`）；两者仅作 `sleeve` 提示层（`oil RSI>80` 提示“金强于油但弱于纳指”；`oil vol低` 提示“金相对纳指 +2.5%”）。
- **复核**：每周 `commodity_pattern_scan.py` 同款 `ahead 10d spread` 重跑 `R1-R5`，本表追加一行；`hold-out 2026-08-08~2027-02-08` 只读验证 `T1b/R3` 不劣化。
- **退出**：若 `n<50` 或 `win<65%`，自动摘提示；`win>70% n>50` 才进 `paper 10%→20%` 观察。

---
*生成：2026-08-24，数据 `daily 518880/513350`，脚本 `commodity_pattern_scan.py` + `sleeve_nav_sim.py` 复核。*

## 周更（本次 2026-09-04 · 全历史重算 672d）

> 口径：SMA-14 RSI（与本文冻结数字对齐：RSI>80 n35/+4.00%/82.9% 逐数复现）。只读 inline，无新脚本文件。

| 条件 → spread | n | win | mean | 可杠杆 |
|---------------|---|-----|------|--------|
| 油 RSI>80 → 金-油 10d | 35 | 82.9% | +4.00% | 历史达标，但 past_year −1.06%，不进 |
| 油 RSI>70 → 金-油 10d | 84（+10） | 72.6% | +3.21% | 同 R4 家族，维持观察 |
| nas mom20<−5% → 金-油 10d | 63 | 71.4% | +4.66% | 无新增触发，维持观察 |

- **今日无触发**：油 RSI 70.6（>70 未到 80）、nas mom20 −1.94%。动作：无。
- 结论不变：R4 高置信成立但 vs sleeve 净≈0，不进杠杆不进 sleeve，仅观察层。
