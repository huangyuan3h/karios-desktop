# 研报覆盖事件诊断（只读 · 2026-09-07）

> 预注册：`docs/designs/research-coverage-prereg-2026-09-07.md`。
> 脚本：`scripts/diag_research_coverage.py`（只读）+ 单测 4 passed。
> **结论：REJECT —— 首覆盖后 10 日相对跑输约 3%，覆盖越密越差。关闭该方向。**

## 0. 先说三个不可测（同样是结论）

- 评级：买入 1486 + 增持 580 vs 中性 9 + 持有 4，零卖出——94.5% 看多，无截面差异，不可测。
- `alpha_score`：每次同步用**当日收盘**重算（`research.py:66`），入库值含未来价格，前视构造，不可回测。
- 目标价隐含涨幅：仅 131 行，thin，不展开。

## 1. 结果表（净成本 market-relative spread，信号=created 日，入场次日 open）

| 事件 | 10d mean / hit / n | 批聚类 | 20d（样本不足，仅报数） |
|------|-------------------|--------|------------------------|
| E1 首覆盖 | **−2.99%** / 31.5 / 248 | −2.76 ± 0.54（11 批） | −7.18% / n=11 |
| E1 含 08-05 回填 | −2.28% / 33.3 / 300 | −2.44 ± 0.59 | −2.84% / n=63 |
| E2 出生周 ≥2 篇 | −3.27% / 29.0 / 100 | −4.28 ± 0.99 | n=2 |
| E2 出生周 =1 篇 | −2.81% / 33.1 / 148 | −2.15 ± 0.55 | n=9 |

- **K1 触发**：首覆盖批聚类 −2.76%，零在线外（负向）。
- **K2 触发**：强度无正向单调（密覆盖 −3.27% 反而差于疏覆盖 −2.81%）。
- 20 日格全 `n<100` underpowered，不做判定，仅记录方向一致。

## 2. 解读

与 Incubator 同族的注意力峰值签名：研报覆盖到达 ≈ 行情兑现之后（被覆盖的本就是茅台/消费/医疗大票），
首覆盖后拿 10 天跑输 3 个点、胜率三成。评级无差异 + 分数前视 + 覆盖反向——研报 α 三条路全关。

## 3. 定位

研报模块保留现状（列表阅读 + 持仓展示），不做任何执行化改造；覆盖/评分不进闸门、不进 paper、不开新预注册。

**执行记录 2026-09-07**：暂停 TIP-012 研报提名合并（`watchlist_automation` research_add，
`RESEARCH_CHANNEL_ENABLED=1` 才恢复，默认关；存量 registry 行不动；S-3 吃全市场分数，
停提名不饿池子）。恢复条件：TIP-012 观察（提名票 vs 同期自然票 10 日 market-relative）
预注册通过。测试：默认暂停 + 显式开启各 1 条（46 passed 同文件）。
同机制的 alpha 通道（TIP-004/TV 提名）列为下一验证候选，本次不动。
