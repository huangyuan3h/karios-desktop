# OPT-143 历史可重放补强 + OPT-144 外围抖动治理  · 归档于 2026-09-04

## 当时的目标（todo 链接）
- `docs/todo.md` P0-5（复盘 5 + P2）→ OPT-143 / OPT-144

## OPT-143 实际做了什么
- 入场缺 14:30 bar 计数 `skipNoPrint1430`（日行 + summary；blotter 不加 kind，
  FE 免改）。三窗重放：OOS2/train/valid 全 0——没有被沉默跳过的候选
- fail-open 清单 7 条进 `strategy-params.md §8`（逐条 verified against code）：
  pool_exit/score_floor/情绪流闸门三处回测-live 同向；卫星出场回退是唯一非对称
  （已量化 ~5%，偏低估）；ST 5% 经评估**无影响**（两引擎 universe 上游已剔 ST，
  不写代码）；除权 258 维持 08-11 定论（正常波动，不打标不删数）
- 覆盖率附录进 `sat-live-caliber-2026-09-04.md §5`：bar_5min 14:30 根
  3319604 行/5254 名/2024-01-02~2026-09-03，三窗 ~5000 名/天

## OPT-144 实际做了什么
- `sync_job_record.consec_failures`（尾随连败计数，永不抛）+ insert_record
  分级 emit：HIGH 即时推；外围单次/双败只进 system_events + hub 日报聚合，
  第 3 次连败才推（payload 带 streak）
- Bark `job_failed` 渲染连败行（"连败 3 次（外围任务连败才推）"）
- 没做的（诚实记录）：apscheduler 层无重试机制，job 都是幂等日 cron——
  "下一 tick 即重试"，没加假重试；`twin_star_reminder` 仍非 HIGH（失败只进
  record+hub，不推手机——OPT-139 已定，要改是产品决策）

## 验证 / 数据
- 新测：`test_trade_calendar_utils` 4 例（is_non_trading_day）、sgap 缺根计数
  1 例、`test_job_failed_severity` 4 例（即时/静默/三连/重置，requires_postgres
  真库 + 自清理）、webhook streak 行 1 例
- 后端全量（待跑，见下）；ruff 无新增；残留 0
- 近 14 天 27 次 job_failed 里外围占 11 次——以后这类只留 record，手机清净

## 后续影响 / 留给谁
- C4 20 笔后：回测-paper 血统第一次对账（回测 fillSrc vs paper entryPxSrc，
  P0-3）
- 长假 freshness 部分狼叫（OPT-142 已记，可接受）
- news 空体 200+error → 422（FE 正常路径无影响）
