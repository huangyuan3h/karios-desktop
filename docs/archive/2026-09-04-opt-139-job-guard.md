# OPT-139 Scheduler 上报统一  · 归档于 2026-09-04

## 当时的目标（todo 链接）
- `docs/todo.md` P0-5（工程 6）→ `docs/optimization-checklist.md` OPT-139
- 15/50 job 无 try、21/50 无 `insert_record`；`twin_star_reminder_job` 自己不写执行记录

## 实际做了什么
- 新增 `scheduler/_job_guard.py`：`record_success` / `record_failure`（永不抛，
  错误截断 500 字）/ `run_guarded`（异常→记失败+打 traceback→回 None）/
  `record_dict_result`（`{"ok"}` 映射，日志回调由 job 自带，原日志逐字保留）
- 迁移 21 个：close_sync / twin_star_reminder（定制：snapshot 降级记
  `|stale-snapshot`、无内容记 `|no-detail`、emit 炸记失败）/ intraday_alarm
  （quotes 失败原来静默，现记失败）/ daily_sync（三分支 None 兼容）/
  hk_industry / cn_industry（包 try，沿用模块级 insert_record，旧测试不动）/
  webhook_delivery / candidate_diff / alpha_radar×2 / adj/daily_basic/
  eastmoney/etf_daily/fund/hk_basic/hk_daily/index_basic/index_daily/macro/stock
- 未动（已达标）：watchlist_automation / paper_twin_star / backtest_recon /
  behavior_audit / twin_star_intraday
- `scheduler/__init__.py`：删 `news_enrich_job` 重复注册（同 id +
  replace_existing，原来无害但迷惑）
- 测试：`test_job_guard.py` 5 例（语义锁死）+ `test_twin_star_reminder_job.py`
  4 例（emit/无内容/emit炸/payload炸四路径）；`test_scheduler_jobs_extra` +
  `test_index_basic_scheduler_job` 加 autouse stub（guard 单点 mock，run() 不写真 DB）

## 验证 / 数据
- `pytest --no-cov -q --timeout=180` → **3680 passed, 3 skipped, 0 failed**
- 测试残留 0（CN:99*/sleeve-test/SMOKE 快照全清）；ruff 仅剩 `__init__.py`
  预存 I001（HEAD 即有，未动）
- 设计教训：`run_guarded` 的 None 既是异常路径又是合法返回——`daily_sync`
  的 svc 可回 None，改显式 try/except，并在 guard docstring 注明约束

## 后续影响 / 留给谁
- OPT-140：`twin_star_reminder` 等失败 record 已有，但 Bark 是否推出去取决于
  severity 映射（reminder 非 HIGH，不推手机，只进 hub/健康页）——要推则改
  `HIGH_JOB_TYPES`，那是 OPT-144 的事
- OPT-144：外围任务失败现在必留 record，`job_failed` 量会涨（可观测性提高的
  代价），正好给 severity 分级提供数据
- alpha_radar 失败日志 warning→exception（含 traceback），排查更易，行为微变已记录
