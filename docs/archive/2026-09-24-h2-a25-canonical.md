# H2-a25 canonical 口径收敛 · 归档于 2026-09-24

## 当时的目标
- 用户要求把当前稳健星舰 a25 固化到系统，统一代码、报告、目录、前端和业务文档口径。
- 目标不包含切换 Live；Live 自动下单仍为港湾 Harbor。

## 实际做了什么
- 将 `starship_robust` 定义统一为卫星 + `cashShare(T−1)` ×（25% H2 套筒 + 75% B3），H2=2pt。
- 评估器增加显式 `h2` / `legacy-canonical` 模式；H2 报告使用生产 ETF 数据加载路径，并独立写入 `sat_h2_a25_2026-09-24.json`。
- 旧 0pt 报告和实验文档保留原数字，增加 historical 标记。
- 目录、shared schema、策略设置、Timeline 标签、Watchlist/模块文档、配方和 SUMMARY 同步 H2-a25 身份及 K3 风险。

## 验证 / 数据
- H2-a25：OOS2/train/valid/long `+238.4/+55.9/+3.2/+738.5`，long MDD `-8.4`、Sharpe `3.50`。
- K1/K2/K4/K5 PASS；K3 FAIL（相对纯 B3 的 long MDD 差 `-1.6pt`，门槛 `-1.0pt`），总状态明确为 REJECT。
- canonical 表示系统唯一现行口径，不表示全门 PASS；未修改卫星信号、B3 配比或 Live 下单链。

## 后续影响 / 留给谁
- 继续观察 H2-a25 的 K3 风险；任何调权重或改门控必须另起预注册。
- H2-a25 进 Live 仍需 paper 20 笔与用户风险授权。
