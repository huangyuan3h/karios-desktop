# OPT-140 双子星模式口径统一  · 归档于 2026-09-04

## 当时的目标（todo 链接）
- `docs/todo.md` P0-5（业务 5）→ `docs/optimization-checklist.md` OPT-140
- `audit_issues` 把 4 只卫星票报"买了不该买"；`execution_card` 推单轨门/金字塔；
  `near_stop` 给卫星票报 S-3 止损线

## 实际做了什么
- 腿真相单点：`notifications._holding_book` 搬进 `twin_star_daily.holding_book`
  （公开），notifications 留委托别名——对账与通知共用同一谓词，腿再也漂移不了
- `live_sat_ts_codes` 并入引擎账本（`sat_book_ts_codes`，按天缓存，45 天 replay
  每天最多一次）：附带修好 pick=STOCK 日引擎已退但用户还持有的卫星票被 S-3
  `near_stop` 误报的根因（之前只有候选+paper-open，没有账本）
- `reconcile_registry(day, mode, leg_ctx)`：twin_star 下卫星腿走引擎账本
  （satExtra/satMissing/satExpected/actualSat），永不计入 S-3 extra；
  single_track 锁死旧行为；`leg_ctx` 可注入，单测不碰线上 pick/引擎
- 落库：Alembic `0040_behavior_audit_sat_leg` + CREATE_SQL；`insert_audit` /
  `latest_audit` 双写 sat 列，并对未迁移老库做读写真降级（回滚后走旧列，
  不炸——migration 仍需 `alembic upgrade head`）
- job/push 文案：`behavior_audit_job` payload 加 sat 摘要（info-only，不触发）；
  Bark `audit_issues` 渲染卫星腿为 🛰 info 行；`execution_card` 标题加
  "·单轨对照"（Bark 已退订，本项管 hub + 文案不误导）
- 前端：`behaviorAudit.ts` 类型 + `BehaviorAuditBanner` 🛰 卫星腿中性面板
  （引擎应持/实持 + 账外持有/应持未持）；mobile ReconcileTab 只读 extraList，
  自动不再误报（sat 面板暂未搬过去）

## 验证 / 数据
- 后端全量 **3684 passed / 0 failed**；前端 **875 passed**；tsc 零报错；ruff 全过
- 新测：`test_behavior_audit_twin_star.py` 3 例（卫星不进 extra / 非 STOCK
  pick 全卫星 / holding_book 五路谓词）+ webhook sat 行 1 例；旧 registry
  测试加 `mode="single_track"` 锁死旧行为
- 测试残留 0；`test_alembic_baseline` head → 0040

## 后续影响 / 留给谁
- 历史 audit 行的 expected/actual 是旧全量口径，新行是核心腿口径——看趋势
  不看绝对值，或重跑 refresh
- `latest_audit`/`insert_audit` 的 pre-0040 降级是过渡兜底，所有环境跑一次
  `alembic upgrade head` 后可删（记入 OPT-142 的迁移卫生）
- mobile ReconcileTab 的 sat 面板、hub 上 execution_card 的"单轨对照"角标
  （目前只有 Bark 标题改了，hub 卡片标题没动）——小尾巴，可进 OPT-142 或
  下次碰 hub 时顺手做
- `expected`/`actual` 现为核心腿计数：之前手机上的"回测应持 0 / 实持 4"
  这类句子以后读作核心腿，不再含卫星
