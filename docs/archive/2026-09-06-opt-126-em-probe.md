# opt-126-em-probe · 归档于 2026-09-06

> 从 `docs/optimization-checklist.md` 原文迁移（只读快照，不回写）。正文 0 条未完成（另有 3 条冬眠：OPT-045/075/127，见正文冬眠区）。

---

### OPT-126：东财出口探针 + 熔断可视化（P1）

**状态**：[x] 2026-09-06

**落地**：
- 新 `service/em_probe.py`：三宿主轻量直连 GET（push2 clist / dataapi bkzj `f62/m:90 t:2` /
  push2his kline，均抄生产参数；`use_proxy=False`，timeout 10s）+ 内存 streak；
  单宿主连败计 3 → `system_events high`（`dedupe_key=em_probe:{host}`，insert 无去重故只发一次）
  + 每次失败都 `emit_event`（下游同日去重，丢投递可重入队）；成功清零；探针自身异常只 warn。
- 新 `scheduler/em_probe_job.py`（`JOB_ID=em_probe`，`IntervalTrigger 10min`）+ 注册进
  `scheduler/__init__.py`；探针失败记自家 job_type，不碰业务 `sync_job_record`（反模式遵守）。
- `em_push2_http.breaker_status()`：`ban_latched / cooldown_remaining_s / fail_streak /
  proxy_degraded` 纯读快照（含过期自动读作 unlatch）。
- `health_routes` 加 `eastmoney_probe` 源（threshold 20min；stale = 未跑过/超 20min/有宿主失败/
  ban 闩锁）+ 横幅字段 `banLatched/cooldownRemainingS/hosts/failingHosts`。
- `industry_fund_flow.py` 无改动：它自带 urllib/curl 栈不走 `em_push2_http`，宿主覆盖已由探针承担；
  `HIGH_JOB_TYPES` 早含 `cn_industry_post_close_sync`（计划注记"已有"属实）。
- `tests/test_em_probe.py` 新 14 用例 + `test_scheduler_jobs_extra` 注册表加 `em_probe`。

**验证**：14 passed；注册表测试归绿；`create_scheduler` 含 `em_probe`（FakeScheduler 断言）。
