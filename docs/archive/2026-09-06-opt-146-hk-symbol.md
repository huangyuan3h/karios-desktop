# opt-146-hk-symbol · 归档于 2026-09-06

> 从 `docs/optimization-checklist.md` 原文迁移（只读快照，不回写）。正文 0 条未完成（另有 3 条冬眠：OPT-045/075/127，见正文冬眠区）。

---

### OPT-146：factor_signals 港股符号误标 + 形态结算语义文档化（P2）

**状态**：[x] 2026-09-06

**落地**：
- `factor_signals_service._symbol_for_ts()`：后缀判市场（`.HK`→`HK:`，其余/无后缀→`CN:`），
  scan 改走它。旧 `f"CN:{code}"` 对港股全错。
- 存量修复 `repair_hk_symbols()` 并已执行：判据 `^CN:[0-9]{5}$`（5 位不可能是 A 股 6 位码）。
  实测 54 行（非条目所写 77，全部 2025-06-16 backfill），`HK:` 孪生 0 → 54 UPDATE、0 删除；
  表总数 113 不变（纯重标，无插入/删除污染）。有孪生时删误标行保 PK。
- `docs/designs/pattern-factor-validation.md` §1 加第 7 条"成交规则（limit-fill-correct v0.2.2）"，
  与 `scripts/repro_scoop_short.py` 头注释同口径（倒挂限价位 low-touch 才成交；同 bar 双触按止损先；
  禁 naive bar-touch，见 hedge-twin §4）。
- `tests/test_factor_signals_symbol.py` 新 5 用例（含 110 天合成 scoop 端到端：HK 出 `HK:00004`、
  CN 出 `CN:600000`；110 天是因为 t-30 处 MA60 需 index≥59）。

**验证**：5 passed；生产表已修复（`CN:[0-9]{5}` 残留 0，`HK:` 54）。
