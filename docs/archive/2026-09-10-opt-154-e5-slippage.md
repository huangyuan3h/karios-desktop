# opt-154-e5-slippage · 归档于 2026-09-10

> 从 `docs/optimization-checklist.md` 原文迁移（只读快照，不回写）。

---

### OPT-154：E5 滑点双计修正（backtest↔live 成本口径对齐 · 用户报障链 2026-09-10）

**状态**：[x] 2026-09-10

**问题**（audit-2026-08-22 E5）：`paper_cost_model` 的 CN 往返成本 30bps 已含 **10bps/边滑点**（HK 90bps 含 15bps/边）；
引擎又在成交价上叠 `slippage_pct=0.05`（5bps/边）→ 回测往返 ≈**40bps**，而 live `paper_s3` 只用成本模型（30bps）。
回测比 live 保守 ~10bps/笔，口径不一致。

**落地**：
- 冻结配置 `slippage_pct 0.05 → 0.0`（CN+HK）：`scripts/run_walk_forward.py`（S3_CONFIG + HK_S3_CONFIG）、
  `scheduler/rolling_oos_job.py`（CN + HK）、`service/reconciliation.py`（CN + HK）。
- `api/backtest_routes.py` `slippage_pct` Query 描述更新（默认 0；成本模型已含滑点，勿双计）。
- 未改 `paper_cost_model` / `paper_s3`（live 本就用模型，口径不变）。

**验证**：三窗 CN **+0.8/+0.6/+0.2pt**（成本降，dd 同降），HK ≈持平，无劣化；
基线重固化 `s3-cn-baseline-20260910`（sha `7d0d9cc414c6`）、`s3-hk-baseline-20260910`（sha `8502178f355b`）；
后端全量 4261 passed（仅 2 既有失败）+ DB 基线干净 + ruff 0。

**不做（残余）**：小票 **ADV/冲击成本**（真实 20–50bps）仍未建模——门的流动性阈值（0.7 亿）只做排除，未做
按额度的冲击滑点。如需真正贴近小票成交，另起一项（按 60 日均额分档加冲击）。
