# opt-147-rwide-hk · 归档于 2026-09-09

> 从 `docs/optimization-checklist.md` 原文迁移（只读快照，不回写）。正文 0 条未完成（另有 3 条冬眠：OPT-045/075/127，见正文冬眠区）。

---

### OPT-147：R-wide breadth 池 HK 污染量级断言（P2 · 只读不断言不修引擎）

**状态**：[x] 2026-09-09（只读诊断 + 回归测试；冻结引擎零改动）

**背景**：`state_bucket_track._load_rows` 无 `sb.market` 过滤，`daily` 池含 ~2800 HK 行。
habit 两道 containment（mv 门、`bar_5min` 零 HK）已验证，但 R-wide breadth 是否含 HK 需量级断言。

**落地**：
- 新 `scripts/assert_rwide_hk.py`（只读，无文件写）：pool census + 三窗
  `breadth_cur`（mv 门）vs `breadth_noHK`（显式去 HK）vs `breadth_withHK`（去 mv 门反事实）。
- `tests/test_state_bucket_track.py::TestHkContainmentOPT147` 新 2 用例（day_all 排除无 mv 名、breadth 与去 HK 版逐数一致）。
- `tests/test_bar_5min.py::test_hk_codes_never_map_to_vendor_opt147`（多格式 HK 永不进 vendor 拉取）。
- `first-principles-2026-09-05.md §二.7` 追加结案行（只增不改）。

**数字**：pool 8025（HK 2785），mv 0 HK，bar_5min 0 HK。
flips(cur-vs-noHK) 三窗 0/0/0 → **关项**；反事实 flips 18/8/5、max 差 0.87（门是承重墙，pattern 复制必带 mv 门）。

**验证**：14 passed（11 state_bucket + 3 bar_5min 内含新用例）；断言脚本真实数据端到端跑通。
