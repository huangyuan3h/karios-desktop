# opt-155-adv-impact · 归档于 2026-09-10

> 从 `docs/optimization-checklist.md` 原文迁移（只读快照，不回写）。

---

### OPT-155：ADV 冲击成本 + 参与率上限（诊断后**关闭** · 当前 AUM 不 material）

**状态**：[x] 2026-09-10（诊断完成 → 结论=不建模）

**背景**：E5（OPT-154）残余——小票 20–50bps 冲击未建模。用户 AUM = **¥1000万**（核心单票 10%=¥100万，
卫星 12.5%=¥125万）。

**诊断**（`scripts/diag_adv_impact.py`，只读；冻结 CN 核心 long 2021-08~2026-08，408 笔）：
- 参与率 `u = 订单名义 / 60日均额`：mean **0.51%** · median 0.42% · p90 **1.04%** · p99 1.44% · max 1.75%；
  **u > 2% = 0 笔**。
- square-root 冲击估计（单边 `C·√(u/1%)`）：C=10 → mean **6.7bps** / p90 10.2bps；C=15 → mean 10.0bps。
- 成本模型（`paper_cost_model`）已含 **10bps/边滑点** → **已覆盖真实冲击**（mean 6.7bps/边 ≪ 10bps，p90 ≈10bps）。

**结论**：**当前 AUM 下 ADV 冲击不 material，不建模**——再加一层明确是 E5 翻版（成本双计）。
成本模型 10bps/边在此规模是合理到偏保守的代理。

**复活条件（写死）**：AUM 增长到 **¥3000万+**（p90 参与率 ≈3%）时重开；诊断脚本已备，直接改 `AUM_YI` 重跑。

**不做**：参与率上限、冲击模型、live 镜像、按均额分档。
