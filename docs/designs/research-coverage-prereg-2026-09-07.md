# 预注册：研报覆盖事件诊断（只读 · 简单版 · 不进 Live）

> 2026-09-07。动机：研报评级 94.5% 看多且零卖出（无截面差异，不可测）；
> `alpha_score` 列用当日收盘重算（`research.py:66` 前视构造，不可回测）；目标价仅 131 行（太 thin）。
> 唯一可测的是**覆盖行为本身**（注意力假说，与 Incubator 同族，方向可能同为反向）。
> 上限：1 个月单 regime 描述性结论；任何结果不进 Live（另需三窗，不可能）。

## 0. 自查（最可能死因）

样本（~1 个月，左截断）/ 单窗（08-09 牛市）/ 共线（被覆盖的全是大票 beta）。
08-05 首日 52 个“首覆盖”是回填左截断非真首发——主口径剔除 08-05 出生，敏感性含它重算。

## 1. 事件与口径

- E1 首覆盖：某票库内第一篇 created 报告日（CST）；信号日 = created 日，入场次日 open，10 日主 / 20 日次。
- E2 强度：出生首周报告数 ≥2 vs =1（描述性）。
- 基准 = 同窗全 A 等权，成本往返 0.3%。market 代码映射：`market=SHANGHAI→SH` 等（`research.py:88`）。

## 2. Kill 线

- K1：首覆盖 10 日批聚类均值 ≤ 0 或零在线内 → 无证据，关闭。
- K2：强度无单调（≥2 不优于 =1）→ 覆盖强度无信息，关闭。
- 任一触发即 REJECT；双过也只是“弱证据”，转长期观察（等 3 个月数据再看），不进 Live。
