# 预注册：闲置现金 ETF 停车场（H-SLEEVE · 2026-09-12）

> **单假设 · 结构选择 · 门槛跑前冻结。** 回到 ETF 层的**原始定位**：**只作用于 S-3 引擎的闲置现金**（替代 0/逆回购），不参与股票仓分配。**因果实现**（进出场用 t-1，赚 t 收益）。
> **关键词**：闲置套筒 停车场 ETF 轮动 因果 S-3 预注册

## 0. 一句话机制（§四 S1）
S-3 常有大比例闲置现金（~40–60%）；把闲置部分按 **mom60+MA200** 择强停进 ETF（金/油/纳指/国债），替代收益≈0 的现金 → 在不改变股票仓的前提下增厚收益；**只在闲置上动手，天然保 S-3 alpha、不抢股票仓**。

## 1. 为什么重开（背景）
- ETF 层**原本**就是"闲置现金的 ETF 增强"（`pick-strong-track.md:39`），历史增量 +3.1/+8.4/+22.3pt。
- 后"择强单轨"把它并成 **100% argmax 同池** → 实测（OPT-177 修正后）**反而不如纯 S-3**。→ 退回"停车场"定位重验。
- **注意**：原闲置套筒模拟 (`portfolio_nav_sim.simulate_sleeve_nav`) 进出场用**当日收盘**（同类前视），故历史增量待重验——本稿用**因果**实现。

## 2. 口径（冻结）
- 基线：**S-3 CN 引擎**（`S3_CONFIG`，window-local，逐窗独立），`engine_nav_by_day`。
- 闲置比例：每日 `idle = 1 − min(1, Σ position_pct)`（用 t-1 快照持仓）。
- **因果**：日 t 的持仓决策用 **t-1 收盘**（MA200 / mom60 / trail），赚 **t** 的 ETF 收益；无 pick → repo=**0**（保守，不假设逆回购收益）。
- 窗口：OOS2/train/valid（+ past_year 2025-08-01~2026-08-21 参考）。

## 3. 变体（预声明，考虑过往 ETF 实验）
| # | 规则 |
|---|------|
| V0 | 基线：闲置收益 0（现状） |
| V1 | 单纳指 `513100`（T6 口径）：close(t-1)>MA200 → 持；否则 0 |
| V2 | **多资产轮动**：{518880 金, 513350 油, 513110 纳指, 511260 国债} 中 close(t-1)>MA200 且 **mom60(t-1) argmax** → 持 |
| V3 | V2 + BOND10 恒入池（低波压舱） |
| V4 | V2 + 因果 trail8（峰值−8%→repo） |

## 4. 判定（冻结）
- **K1**：各变体在 **OOS2/train/valid 三窗增量 ≥ 0**（vs V0）。
- **K2**：全窗合计增量 **> 0**。
- K1&K2 → 记"闲置停车场 ETF 层可用"（可议落产品，须再证实盘口径）；否则 REJECT。
- **不扫**：候选清单/lookback/MA/trail 阈值（V2 用 60/200/8 沿用历史）。

## 5. 死因预判（跑前写死）
- 主：`#2 共线`（闲置期恰是弱市，ETF 也可能跌）；`#4 regime`。
- 次：ETF 历史仅 2023+（OIL）/数据覆盖；成本（ETF 0.5bp 换手）。

## 6. 产出
- 脚本：`scripts/eval_idle_sleeve.py` → `data/backtest_reports/idle_sleeve_2026-09-12.json`
- 结论落 `docs/backtests/stable/idle-sleeve-2026-09-12.md` + SUMMARY。

## 结果（跑后补）
**可用 = {V2, V3, V4}**；V1（单纳指）valid −3.7 出局。增量：V2 **+11.2/+13.2/+2.7**；**V4（多资产+因果trail8）最平滑 +11.2/+11.8/+11.4**；past_year +7.2~8.7。基线三窗与官方一致（+46.5/+34.4/+38.7）→ 口径正确。原闲置套筒模拟有同类前视，改因果后仍全正 → 历史结论存活。见 [`idle-sleeve-2026-09-12.md`](../backtests/stable/idle-sleeve-2026-09-12.md)。

*冻结于 2026-09-12，跑数前。*
