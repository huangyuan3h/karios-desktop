# 预注册：市况加权卫星 60/40 vs 冻结50/50（2026-09-05）

> 状态：待跑。跑完结论进`docs/backtests/sat-weight-6040-2026-09-05.md`（PASS或REJECT都留档）；任何情况下不自动进Live，进不进用户另拍。

## 1. 假设（原生一句话）

牛市才配多吃卫星：up市卫星权重60/40，其余维持50/50（H1）。依据`sat-regime-2026-09-04`：up三窗全赚最稳，choppy次之，down被R-wide拦。

## 2. 冻结细则（跑之前定死，跑中不改）

- 市况：只用趋势维（上证指数`index_daily`之`000001.SH` 20日涨跌，`>+3%` up / `<−3%` down / else choppy），与G1逐字同口径（注意：`000001.SH`是指数代码，`daily`股票表里没有它，脚本已踩过此坑）；波动率维禁用（G1结论#2）。
- 权重（只测两个， ladder外不加档）：
  - V1（主）：up 0.6 / choppy 0.5 / down 0.5（只动up，改动面最小）
  - V2（备）：up 0.6 / choppy 0.4 / down 0.4（测非up值不值得满权）
- 基线：冻结习惯50/50 twin（同窗同配方，`C1 3% + same_1430 + body=3 + 第3日14:30卖`，gross）。
- 窗口：`OOS2/train/valid`（拒收闸）+ `2022熊市`（软闸）；`2021/2023`描述性。
- 实现：只读post-hoc重混（replay日序列 + 市况序列加权），不动引擎不改参；gross + 沿用C的base/stress抖动预算列示。

## 3. 拒收线（任一触发即REJECT）

1. `OOS2/train/valid` 任一窗 twin-tot相对基线差 `< −5pt`（沿用`REJECT_PT=5.0`）。
2. 任一窗 sharpe相对基线差 `< −0.3`。
3. `2022` 相对基线差 `< −5pt`（软闸，熊市加权不能比躺平差一截）。
4. V1/V2双杀（都不显著为正）→ 方向证伪，不开第三组权重（防 минуты调参）。

## 4. 防过拟合手段

- 市况定义抄G1（09-04冻结，早于本想法），维度1个，档位2个，无网格。
- 选参与验证窗同一批公布（OOS2/train/valid同为闸，无train选valid验的假分层——如实声明，不伪装walk-forward）。
- down档位 mechanical上几乎不触发（R-wide已关），如实声明，不算“测过熊市加权”。
